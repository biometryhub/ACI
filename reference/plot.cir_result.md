# Plot a causal influence range result

Plot a causal influence range result

## Usage

``` r
# S3 method for class 'cir_result'
plot(x, ...)
```

## Arguments

- x:

  A `cir_result` object.

- ...:

  Passed to the underlying base-graphics calls.

## Value

`x`, invisibly; called for the plot it draws.
