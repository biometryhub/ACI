% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-core.R
\name{aci}
\alias{aci}
\title{Assimilative causal inference}
\usage{
aci(
  model,
  obs,
  engine = c("auto", "cgns"),
  conditional = NULL,
  table = NULL,
  keep = c("paths", "table", "none"),
  decompose = TRUE,
  init = NULL,
  stepper = c("explicit", "implicit"),
  nsub = 1L,
  regularize = NULL,
  loglik = TRUE,
  ...
)
}
\arguments{
\item{model}{A \code{cgns_model} object.}

\item{obs}{An observed trajectory, or anything \code{\link[=as_obs]{as_obs()}} accepts.}

\item{engine}{One of \code{"auto"} or \code{"cgns"}; \code{"auto"} selects the closed-form
engine for a conditional-Gaussian model.}

\item{conditional}{Optional \code{aci_conditional_spec}; see \code{\link[=aci_conditional]{aci_conditional()}}.}

\item{table}{Optional precomputed \code{lag_table}, whose online diagonal is used
in place of the headline smoother.}

\item{keep}{One of \code{"paths"}, \code{"table"} or \code{"none"}, selecting which objects
are retained on the result. It does not select a smoother.}

\item{decompose}{\code{TRUE} to retain the signal and dispersion parts.}

\item{init}{Optional list with the initial hidden \code{mean} and \code{cov}.}

\item{stepper}{Either \code{"explicit"} or \code{"implicit"}.}

\item{nsub}{Positive whole number of sub-steps taken per observation.}

\item{regularize}{Covariance policy for this call; see \code{\link[=aci_filter]{aci_filter()}}. One
record covers the filter, the smoother and any table this call builds, and
is returned in \code{meta$regularization}.}

\item{loglik}{\code{TRUE} (the default) accumulates the predictive
log-likelihood on the internal filter, where \code{keep = "paths"} exposes it as
\code{paths$filter$meta$loglik}. ACI itself never uses it, so \code{FALSE} skips that
work and leaves \code{paths$filter$meta$loglik} \code{NULL}; every ACI quantity is
unchanged.}

\item{...}{Must be empty; unused arguments are an error.}
}
\value{
An object of class \code{aci_result}.
}
\description{
Runs the filter and smoother for a model and observed record and scores each
time by the relative entropy of the smoother from the filter. A normal
\code{aci()} call uses the supplied-code backward-ODE headline smoother, including
its correlated-noise correction, independently of \code{keep}. \code{\link[=lag_table]{lag_table()}} and
\code{aci(table = ...)} instead use the complete online Theorem 3 smoother;
their finite-grid diagonal can therefore differ from headline ACI.
}
\examples{
m <- aci_dyad_model()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
ob <- as_obs(sim)
a <- aci(m, ob)
a

}
\seealso{
\code{\link[=aci_range]{aci_range()}}, \code{\link[=lag_table]{lag_table()}}, \code{\link[=aci_metric]{aci_metric()}}
}
