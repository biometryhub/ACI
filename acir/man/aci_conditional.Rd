% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-assimilation.R
\name{aci_conditional}
\alias{aci_conditional}
\title{Conditional ACI specification}
\usage{
aci_conditional(
  given = NULL,
  method = c("mask", "reduce"),
  target = NULL,
  first_step = c("uniform", "matlab")
)
}
\arguments{
\item{given}{Integer or character vector naming the conditioning observed
channels \code{x_B}. Supply this or \code{target}, not both.}

\item{method}{Either \code{"mask"}, which gives the conditioning channels'
innovations zero weight in the filter, or \code{"reduce"}, which substitutes
them as known forcing.}

\item{target}{Integer or character vector naming the target observed
channels \code{x_A}. Supply this or \code{given}, not both.}

\item{first_step}{Either \code{"uniform"}, which masks the observation precision
at every step, or \code{"matlab"}, which leaves the first slice unmasked as the
reference scripts do. \code{"matlab"} requires \code{method = "mask"}.}
}
\value{
An object of class \code{aci_conditional_spec}.
}
\description{
Describes which observed channels carry the causal question and which are
conditioned out of it, and by which method. \code{aci_conditional()} describes
conditional ACI masking; \code{\link[=aci_conditional_reduce]{aci_conditional_reduce()}} is the model reduction
\code{method = "reduce"} asks for.
}
\section{Estimand}{
Split the observed process into target and conditioning channels,
\verb{x = (x_A, x_B)}, with hidden process \code{y}. Conditional ACI is the estimand
\code{y(t) -> x_A | x_B}: only the target channels \code{x_A} transfer information
into the hidden posterior, and the conditioning channels \code{x_B} are
conditioned upon rather than assimilated. \code{"mask"} realises it by giving
the \code{x_B} innovations zero weight in the filter gain, the Riccati term and
the online-smoother gain, through an observation-precision matrix supported
only on the \code{A} block; \code{"reduce"} realises it by rewriting the
model so \code{x_B} enters as a known time series (prescribed forcing). The two
coincide when the \code{A}-\code{B} noise cross-block vanishes, which the reduction
checks along the whole path.

\code{target} names \code{x_A} directly, which is how the reference scripts write the
question (\verb{h_W(t) -> T_C | (u, T_E, tau, I)},
\code{ENSO_model_cond_ACI_h_W_unobs.m:1199-1202}). \code{given} names the complement
\code{x_B}. Supply exactly one; the other side is derived.
}

\section{First-slice convention}{
The reference scripts fill the first slice of the observation-precision
array with the full Gram inverse before the target-only overwrite, and mask
only the later slices
(\code{ENSO_model_cond_ACI_h_W_unobs.m:1197} against \verb{:1250}). \code{first_step}
selects between masking every slice, \code{"uniform"}, and reproducing that
asymmetry, \code{"matlab"}. It is not a round-off-level choice: on a 4001-point
ENSO path with \code{h_W} hidden and \code{T_C} the target it moves the step-2 filter
mean by 0.108 and the peak ACI by 0.574, and the difference decays through
the record rather than vanishing. It is inert wherever the mask itself is
inert. \code{"matlab"} applies only to \code{"mask"}, which is where a masked
precision path exists.
}

\examples{
aci_conditional(given = 2, method = "mask")
aci_conditional(target = 1, method = "mask")

}
\seealso{
\code{\link[=aci_conditional_reduce]{aci_conditional_reduce()}}, \code{\link[=aci_filter]{aci_filter()}}
}
