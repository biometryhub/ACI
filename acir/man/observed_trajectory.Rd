% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-utils.R
\name{observed_trajectory}
\alias{observed_trajectory}
\title{Observed trajectory on a uniform time grid}
\usage{
observed_trajectory(t, x, noise_free = TRUE, names = NULL)
}
\arguments{
\item{t}{Numeric vector of observation times, strictly increasing and
uniformly spaced.}

\item{x}{Numeric matrix of observations with one row per time and at least
one column, or a vector coerced to a single column.}

\item{noise_free}{Logical; must be \code{TRUE}, since noisy observations are not
supported in this version.}

\item{names}{Optional character vector of unique, non-empty column names,
one per observed channel.}
}
\value{
An object of class \code{obs_traj}: a list with the time vector \code{t}, the
observation matrix \code{x}, the step \code{dt}, the observed dimension \code{k} and the
flag \code{noise_free}.
}
\description{
Constructs the observation object consumed throughout the package. The grid
must be strictly increasing and uniformly spaced, and the observations must
be finite; version 0 additionally assumes the observations are effectively
noise-free. The noise-free restriction follows the method's current
published scope: andreou2026cir Section 2.1 leaves noise-contaminated
observations to future work, and its Discussion lists them as an open
direction, so this is a limitation of the framework as published, not a
modelling assumption added by the package.
}
\examples{
observed_trajectory(t = seq(0, 1, by = 0.1),
                    x = matrix(rnorm(11), ncol = 1))

}
\seealso{
\code{\link[=as_obs]{as_obs()}}, \code{\link[=aci_filter]{aci_filter()}}
}
