% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-core.R
\name{aci_range}
\alias{aci_range}
\alias{aci_range.lag_table}
\alias{aci_range.aci_result}
\title{Causal influence range}
\usage{
aci_range(x, direction = c("forward", "backward"), ...)

\method{aci_range}{lag_table}(
  x,
  direction = c("forward", "backward"),
  method = c("exact", "l1_linf"),
  epsilon = NULL,
  min_M = "auto",
  masked_value = c("na", "zero"),
  quadrature = c("simpson", "sum", "matlab_eps_grid"),
  simpson_close = c("quadratic", "trapezoid"),
  anchors = NULL,
  convention = c("count", "lag_time"),
  epsilon_grid = NULL,
  ...
)

\method{aci_range}{aci_result}(x, direction = c("forward", "backward"), ...)
}
\arguments{
\item{x}{A \code{lag_table} or \code{aci_result} object.}

\item{direction}{\code{"forward"}, the only direction in this release. See above.}

\item{...}{Arguments passed to methods.}

\item{method}{The objective functional. \code{"exact"} is the definitional
objective range, the subjective range averaged over every threshold;
reading the range off the running maximum makes that average a finite
sum, \verb{dt * sum(suffix max) / M}, with no quadrature error. \code{"l1_linf"} is
the efficient ratio the ACI reference script computes, \code{dt * integral(row) / M}. They are different functionals, not two quadratures of one: they
coincide only where the divergence decreases with lag.}

\item{epsilon}{Optional numeric vector of finite non-negative
thresholds at which
the subjective range is also reported. Its read-out convention is set by
\code{convention}. This vector reports thresholds and nothing else; the
MATLAB-compatibility quadrature takes its nodes from \code{epsilon_grid}, a
separate argument, so that one vector is never asked to do both jobs.}

\item{min_M}{Either \code{"auto"} or one finite non-negative number; ranges whose
strength falls below it are masked. It also sets the strength floor the
\code{status} vocabulary is judged against, and \code{min_M = NULL} turns off the
masking but not the status, which then falls back to the
\code{aci.cir_min_strength} option.}

\item{masked_value}{What a masked range reports: \code{"na"} (the default) keeps
the masking visible as \code{NA}; \code{"zero"} follows andreou2026cir Remarks B.4
and C.4 and the FBCIR scripts, which set the length to 0. The paper's
forward figures are computed on an untruncated table; the adaptive
truncation of the default \code{lag_table()} is a package storage device whose
use is recorded in the result's \code{bound} label, and \code{tol = 0} reproduces
the untruncated convention.}

\item{quadrature}{How a row is reduced to its objective range, recorded in
\code{meta$quadrature}. For \code{method = "l1_linf"}: \code{"simpson"} (the default)
follows the ACI and EnKBS codebases' active lines, and \code{"sum"} is the
literal andreou2026cir eq. G.8 L1 grid-function sum, which the FBCIR
scripts' active lines use. For \code{method = "exact"} the threshold average is
a finite sum, so there is no time-axis quadrature to choose and
\code{"simpson"} and \code{"sum"} return the same exact value; \code{"matlab_eps_grid"}
instead reproduces the reference script's \code{defn_objective_CIR}, a Simpson
quadrature of the subjective read-out over the threshold grid
\code{epsilon_grid}.
\code{"matlab_eps_grid"} is defined for \code{method = "exact"} only.}

\item{simpson_close}{Closing rule for the leftover interval when a grid has
an even number of points: \code{"quadratic"} (the default) fits a quadratic
through the last three points, following \code{simps.m} in the ACI reference
code; \code{"trapezoid"} is the package's pre-0.0.21 rule and reproduces
results reported by earlier versions. Odd-length grids are unaffected.}

\item{anchors}{Optional whole numbers indexing the anchor times to report;
\code{NULL} (the default) reports every anchor time. Only the requested rows
are formed and reduced, so a handful of anchors on a long record costs a
fraction of the whole-record computation. The result is subset to the
anchors asked for, in the order asked for, and records them in
\code{meta$anchors}.}

\item{convention}{The subjective read-out. \code{"count"} (the default) is the
ACI reference script's \code{subj_CIR_idx * dt}, the 1-based index of the last
exceedance times the step. \code{"lag_time"} is andreou2026cir eq. G.7, the lag
time of that exceedance with the first cell at lag 0, one grid step below.
Cells with no exceedance report 0 under both. The objective range is not
affected: \code{"exact"} and \code{"matlab_eps_grid"} both average the counting
read-out, which is the convention the definition is written in.}

\item{epsilon_grid}{Threshold quadrature nodes for
\code{quadrature = "matlab_eps_grid"}, strictly increasing. \code{NULL} (the
default) uses the reference script's 513-point grid, \code{10^seq(-6, 0.5, length.out = 513)}. Supplying it with any other \code{quadrature} is an error,
so the reporting thresholds in \code{epsilon} and these quadrature nodes
can never
be taken from one vector.}
}
\value{
An object of class \code{cir_result} with \code{direction} \code{"forward"}.
}
\description{
Summarizes the duration of influence on the discrete time grid, forward from
each anchor time. A finite adaptive table is labelled
\code{objective_on_truncated_table}; its \code{tail_bound} field is a heuristic tail
estimate and must not be interpreted as a certified error bound. The
\code{l1_linf} estimators are ratios, integrated with composite Simpson over the
whole span, following the ACI reference code.
}
\details{
Only \code{direction = "forward"} is in this release. The backward range is a
\code{FBCIR_code-main} feature: it appears in no ACI_code script, and it is held
in the development reserve (\verb{reserve/fbcir/}) for the release that brings
that family in. \code{direction = "backward"} raises \code{aci_error_not_implemented}
rather than returning a forward answer under a backward label.
}
\section{Methods (by class)}{
\itemize{
\item \code{aci_range(lag_table)}: Forward range from a precomputed lag table.

\item \code{aci_range(aci_result)}: Forward range from an ACI result, building the table
when it was not retained. The covariance policy is not re-resolved here:
it is read off the result's own \code{meta$regularization}, so re-analysing a
saved result in a new session cannot silently change it.

}}
\examples{
m <- aci_dyad_model()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
ob <- as_obs(sim)
tb <- lag_table(m, ob, mode = "forward")
aci_range(tb)

}
\references{
Andreou, M. and Chen, N. (2026). Bridging prediction and attribution:
identifying forward and backward causal influence ranges using assimilative
causal inference. arXiv:2510.21889v2, 4 August 2026.
\doi{10.48550/arXiv.2510.21889}
}
\seealso{
\code{\link[=lag_table]{lag_table()}}
}
