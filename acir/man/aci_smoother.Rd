% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-assimilation.R
\name{aci_smoother}
\alias{aci_smoother}
\alias{aci_smoother.cgns_model}
\alias{aci_smoother.stochastic_model}
\title{Data assimilation smoother}
\usage{
aci_smoother(model, obs, ...)

\method{aci_smoother}{cgns_model}(
  model,
  obs,
  filter = NULL,
  conditional = NULL,
  init = NULL,
  stepper = c("explicit", "implicit"),
  nsub = 1L,
  regularize = NULL,
  force_validate = FALSE,
  ...
)

\method{aci_smoother}{stochastic_model}(model, obs, ...)
}
\arguments{
\item{model}{A \code{cgns_model} or \code{stochastic_model} object.}

\item{obs}{An observed trajectory, or anything \code{\link[=as_obs]{as_obs()}} accepts.}

\item{...}{Arguments passed to methods.}

\item{filter}{Optional precomputed filter path; recomputed when \code{NULL}.}

\item{conditional}{Optional \code{aci_conditional_spec} selecting a
conditional ACI
reduction; see \code{\link[=aci_conditional]{aci_conditional()}}.}

\item{init}{Optional list with the initial hidden \code{mean} and \code{cov}.}

\item{stepper}{Either \code{"explicit"} or \code{"implicit"}.}

\item{nsub}{Positive whole number of sub-steps taken per observation.}

\item{regularize}{Covariance policy for this call; see \code{\link[=aci_filter]{aci_filter()}}. One
record covers the whole call, so a filter recomputed here and the backward
recursion that consumes it share the \code{meta$regularization} on the returned
smoother.}

\item{force_validate}{\code{FALSE} (the default) lets a \code{filter} that
\code{\link[=aci_filter]{aci_filter()}} produced for this same model, observations and conditional
specification, and that has not been altered since, skip the per-step
re-validation of its covariances. Any other supplied path, including one
that has been through \code{saveRDS()}, is validated in full as before. \code{TRUE}
validates unconditionally. The smoother result is the same either way.}
}
\value{
An assimilation path: \code{da_path_gaussian} for the closed-form engine.
}
\description{
Generic reconstructing the hidden state from the whole observed record. The
closed-form method is used for a \code{cgns_model}; a general \code{stochastic_model}
is out of scope in this release.
}
\section{Methods (by class)}{
\itemize{
\item \code{aci_smoother(cgns_model)}: Closed-form backward-ODE smoother for a
conditional-Gaussian model.

\item \code{aci_smoother(stochastic_model)}: Classed not-implemented condition for a general
(non-CGNS) stochastic model.

}}
\examples{
m <- aci_dyad_model()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
ob <- as_obs(sim)
f <- aci_filter(m, ob)
aci_smoother(m, ob, filter = f)

}
\seealso{
\code{\link[=aci_filter]{aci_filter()}}, \code{\link[=aci]{aci()}}
}
