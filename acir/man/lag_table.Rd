% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-assimilation.R
\name{lag_table}
\alias{lag_table}
\title{Finite-lag divergence table}
\usage{
lag_table(
  model,
  obs,
  mode = c("forward", "full"),
  tol = getOption("aci.default_tol", 1e-08),
  window = 3L,
  max_lag = Inf,
  filter = NULL,
  smoother = NULL,
  conditional = NULL,
  init = NULL,
  stepper = "explicit",
  nsub = 1L,
  regularize = NULL,
  ...
)
}
\arguments{
\item{model}{A \code{cgns_model} object.}

\item{obs}{An observed trajectory, or anything \code{\link[=as_obs]{as_obs()}} accepts.}

\item{mode}{Either \code{"forward"} or \code{"full"}, selecting which cells are
retained.}

\item{tol}{Positive tolerance below which a row is frozen by the adaptive
storage rule.}

\item{window}{Number of consecutive steps a row must stay below \code{tol} before
it is frozen.}

\item{max_lag}{Maximum positive lag retained, or \code{Inf} for no cap.}

\item{filter}{Optional precomputed filter path.}

\item{smoother}{Optional precomputed smoother path.}

\item{conditional}{Optional \code{aci_conditional_spec}; see \code{\link[=aci_conditional]{aci_conditional()}}.}

\item{init}{Optional list with the initial hidden \code{mean} and \code{cov}.}

\item{stepper}{Either \code{"explicit"} or \code{"implicit"}.}

\item{nsub}{Positive whole number of sub-steps taken per observation.}

\item{regularize}{Covariance policy for this call; see \code{\link[=aci_filter]{aci_filter()}}. One
record covers the filter, the Theorem 3 reference smoother and every
relative-entropy denominator the table forms, and is returned in
\code{meta$regularization}.}

\item{...}{Must be empty; unused arguments are an error.}
}
\value{
An object of class \code{lag_table}.
}
\description{
Builds the table of finite-lag divergences consumed by the causal
influence range estimators. A lag table uses the complete online Theorem 3
smoother as its reference. That reference costs O(N) time-point work; table
construction then costs work proportional to the retained lag cells, with
O(N^2) cells for a full table in the worst case.
}
\examples{
m <- aci_dyad_model()
sim <- simulate(m, seed = 1, t_end = 2, dt = 0.01)
ob <- as_obs(sim)
lag_table(m, ob, mode = "forward")

}
\references{
Andreou, M. and Chen, N. (2026). Bridging prediction and attribution:
identifying forward and backward causal influence ranges using assimilative
causal inference. arXiv:2510.21889v2, 4 August 2026.
\doi{10.48550/arXiv.2510.21889}

Andreou, M., Chen, N. and Li, Y. (2026). An adaptive online smoother with
closed-form solutions and information-theoretic lag selection for
conditional Gaussian nonlinear systems. \emph{Journal of Nonlinear Science}
\strong{36}(4), 71. \doi{10.1007/s00332-026-10271-x}
}
\seealso{
\code{\link[=aci_range]{aci_range()}}, \code{\link[=lt_row]{lt_row()}}
}
