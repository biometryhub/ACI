% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-model-library.R
\name{aci_enso_model}
\alias{aci_enso_model}
\title{Six-variable stochastic conceptual ENSO model (andreou2026aci SI via
chen2022enso eqs. 1a-1f).}
\usage{
aci_enso_model(
  hidden = c("hW", "tau"),
  sigma_E = NULL,
  lambda = 2/60,
  params = NULL,
  variant = "aci_code",
  observations = c("reduced", "full"),
  approximation = c("exact", "zeroth_order_c1"),
  prescribed = NULL,
  matlab_defect_compat = FALSE
)
}
\arguments{
\item{hidden}{Character vector naming hidden ENSO variables.}

\item{sigma_E}{Eastern-Pacific temperature noise amplitude.}

\item{lambda}{Decay rate for the diversity index.}

\item{params}{Optional complete parameter list overriding the preset.}

\item{variant}{Parameter and coefficient convention.}

\item{observations}{Observation set the estimand is defined on, \code{"reduced"}
or \code{"full"}. \code{"reduced"} is defined for \code{hidden = "tau"} and
\code{hidden = "TC"}, where it is the default and, for \code{"TC"}, the only value;
every other partition is \code{"full"}. For \code{hidden = "tau"} observations are
still supplied on all five observed channels either way, and \code{"reduced"}
prescribes \code{u} and \code{h_W} from them instead of assimilating them.}

\item{approximation}{Either \code{"exact"}, the conditionally linear split of the
six-state drift, or \code{"zeroth_order_c1"}, the \code{T_C}-hidden substitution
\code{c1(t, TC) -> c1(t, 0)} described above. \code{"zeroth_order_c1"} is defined
only for \code{hidden = "TC"}, and \code{hidden = "TC"} requires it.}

\item{prescribed}{For \code{hidden = "TC"} only, and then required: a data frame
or named list carrying \code{t}, \code{u}, \code{hW} and \code{tau} as equal-length numeric
series on one strictly increasing uniform time grid. Other elements, such
as the remaining channels of a simulated path, are ignored.}

\item{matlab_defect_compat}{For \code{hidden = "TC"} only. The reference script's
assimilation forcing \code{f_y} (\verb{ENSO_model_cond_ACI_T_C_unobs.m:1053,:1151})
omits the thermocline term \code{gamma_C * h_W}, which the same script's
simulator drift (\verb{:1124}) includes and which the sibling \code{ACI_code}
scripts carry in the corresponding \code{T_C} coefficient rows. \code{h_W} is
prescribed and observed here, so the term is available, and \code{acir}
includes it. Set \code{TRUE} to reproduce the published script verbatim. On a
14-model-year window the two differ by up to \code{0.105} in the filter mean
and \code{2.75} in ACI, and the time-integrated ACI roughly doubles; filter and
smoother covariances are identical, because the term enters only the mean
equations.}
}
\description{
Constructs the six-variable ENSO system split into an observed and a hidden
component at \code{hidden}. \code{I} must remain observed, and so must \code{TC} unless the
zeroth-order approximation below is requested. The coefficients are the
fixed-state ones of the ACI_code conditional ENSO scripts.
}
\section{T_C hidden (zeroth-order)}{
The six-state ENSO system is not conditionally Gaussian with \code{T_C} hidden:
the damping \verb{c_1(t, T_C) T_C} is cubic in \code{T_C}. \code{ACI_code}'s
\code{ENSO_model_cond_ACI_T_C_unobs.m} restores conditional linearity by a
zeroth-order Taylor expansion of \code{c_1} about the climatology \code{T_C = 0},
replacing the state-dependent damping with the time-only series
\code{r_C - c_1(t, 0)}. \code{approximation = "zeroth_order_c1"} builds that inference
model: observed \verb{(T_E, I)}, hidden \code{T_C}, with \code{u}, \code{h_W} and \code{tau} entering
as prescribed forcings from their observed series, supplied through
\code{prescribed}. This is an approximation of the system, not a re-split of it -
the simulator keeps the full nonlinear \code{c_1(t, T_C)}, and the filter and
smoother moments are those of the approximating model. \code{simulate()} is
refused on the result for that reason; generate a path from
\code{aci_enso_model(hidden = c("u", "hW", "tau"))} and build this model from it.

The prescribed series are looked up by index on the grid they were supplied
on, never interpolated, and assimilation refuses an observation grid that is
not that grid.
}

\section{Observation set for the tau partition}{
\code{hidden = "tau"} has two estimands, and they are not the same causal
quantity. \code{observations = "reduced"}, the default for that partition, is the
reference script's: the observed process is \verb{(T_C, T_E, I)}, and \code{u} and
\code{h_W} enter the target drifts as prescribed known time series rather than as
assimilated channels (\code{ENSO_model_cond_ACI_tau_unobs.m:1020-1039},
\verb{:1136-1141}). \code{observations = "full"} assimilates all five observed
channels \verb{(u, h_W, T_C, T_E, I)} and so uses strictly more information: both
prescribed drifts carry \code{tau} (\code{Lx[u] = -0.0407}, \code{Lx[h_W] = -0.0814}), and
prescribing them reproduces their effect on the \code{T_C}/\code{T_E} drift but not
their own innovations.

The script asserts the two agree. On a 4001-point source-derived path they
do not: filter means differ by up to 0.247, the ACI series by up to 0.776 -
about three times its own mean level, with Pearson correlation 0.905 -
while the time-averaged ACI agrees to within 0.5\%. The reduction preserves
the average level and distorts the time-resolved curve, which is the
reported quantity. Any fidelity claim must name which observation set it
reproduces. \code{meta$observations} records which one a model carries.
}

\examples{
aci_enso_model()

# The T_C-hidden partition is not self-contained: it needs the u, h_W and
# tau series it treats as prescribed forcings.
sim <- simulate(aci_enso_model(hidden = c("u", "hW", "tau")),
                seed = 1, T = 1, dt = 0.005)
path <- data.frame(t = sim$obs$t, u = sim$hidden[, 1],
                   hW = sim$hidden[, 2], tau = sim$hidden[, 3])
aci_enso_model(hidden = "TC", approximation = "zeroth_order_c1",
            prescribed = path)

}
\references{
Andreou, M., Chen, N. and Bollt, E. (2026). Assimilative causal inference.
\emph{Nature Communications} \strong{17}, 1854. \doi{10.1038/s41467-026-68568-0}

Chen, N., Fang, X. and Yu, J.-Y. (2022). A multiscale model for El Nino
complexity. \emph{npj Climate and Atmospheric Science} \strong{5}, 16. arXiv:2104.07174.
}
