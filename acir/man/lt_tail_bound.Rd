% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-assimilation.R
\name{lt_tail_bound}
\alias{lt_tail_bound}
\title{Heuristic tail estimate of a lag table}
\usage{
lt_tail_bound(x, j = NULL)
}
\arguments{
\item{x}{A \code{lag_table} object.}

\item{j}{Optional integer index of a single anchor time; \code{NULL} returns the
estimate at every time.}
}
\value{
Numeric vector of heuristic tail estimates.
}
\description{
The historical \code{lt_tail_bound()} name is retained for compatibility, but its
value is a heuristic tail estimate, not a certified mathematical error bound.
}
\examples{
m <- aci_dyad_model()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
ob <- as_obs(sim)
tb <- lag_table(m, ob, mode = "forward")
head(lt_tail_bound(tb))

}
