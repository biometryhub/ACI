% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-model.R
\name{aci_simulate}
\alias{aci_simulate}
\title{Simulate a path from a model}
\usage{
aci_simulate(object, ...)
}
\arguments{
\item{object}{A \code{stochastic_model} or \code{cgns_model} object.}

\item{...}{Passed to the \code{simulate()} method: \code{nsim}, \code{seed}, \code{t_end}, \code{dt},
\code{ic}, \code{burn_in}, \code{keep_hidden}, \code{keep_noise}.}
}
\value{
Whatever the method returns: an \code{aci_sim} object, or a list of
them when \code{nsim} exceeds one.
}
\description{
The named verb for the package's \code{\link[=simulate.stochastic_model]{simulate.stochastic_model()}} method. It
exists so that the public surface reads as one family of \verb{aci_*} verbs. It
is the same call, with the same arguments and the same draws, and the S3
method stays registered on the \code{stats::simulate()} generic, so
\code{simulate(model)} keeps working.
}
\examples{
m <- aci_dyad_model()
aci_simulate(m, seed = 1, t_end = 2, dt = 0.01)

}
\seealso{
\code{\link[=simulate.stochastic_model]{simulate.stochastic_model()}}, \code{\link[=aci_model]{aci_model()}}
}
