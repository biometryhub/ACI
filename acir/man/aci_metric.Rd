% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-core.R
\name{aci_metric}
\alias{aci_metric}
\title{Gaussian relative entropy along a pair of paths}
\usage{
aci_metric(p, q, decompose = TRUE)
}
\arguments{
\item{p}{A \code{da_path_gaussian} object, the integrating distribution.}

\item{q}{A \code{da_path_gaussian} object on the same time grid.}

\item{decompose}{\code{TRUE} to return the signal and dispersion parts alongside
the total.}
}
\value{
A data frame with the time column \code{t} and either \code{total} alone or
\code{total}, \code{signal} and \code{dispersion}.
}
\description{
Evaluates the relative entropy at every time of two assimilation paths on a
common grid. Gaussian relative entropy is oriented as smoother relative to
filter.
}
\examples{
m <- aci_dyad_model()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
ob <- as_obs(sim)
f <- aci_filter(m, ob)
s <- aci_smoother(m, ob, filter = f)
head(aci_metric(s, f))

}
\seealso{
\code{\link[=aci_metric_pair]{aci_metric_pair()}}, \code{\link[=aci]{aci()}}
}
