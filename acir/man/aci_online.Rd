% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-assimilation.R
\name{aci_online}
\alias{aci_online}
\alias{aci_online.cgns_model}
\alias{aci_online.stochastic_model}
\title{Fixed-lag online data assimilation}
\usage{
aci_online(model, obs, lag, ...)

\method{aci_online}{cgns_model}(
  model,
  obs,
  lag,
  filter = NULL,
  conditional = NULL,
  init = NULL,
  regularize = NULL,
  force_validate = FALSE,
  ...
)

\method{aci_online}{stochastic_model}(model, obs, lag, ...)
}
\arguments{
\item{model}{A \code{cgns_model} or \code{stochastic_model} object.}

\item{obs}{An observed trajectory, or anything \code{\link[=as_obs]{as_obs()}} accepts.}

\item{lag}{Number of future steps each estimate may condition on. A
non-negative whole number, or \code{Inf} for the whole record. No default: the
lag is the argument the function exists for, and defaulting it invites the
full-lag result to be mistaken for \code{\link[=aci_smoother]{aci_smoother()}}.}

\item{...}{Arguments passed to methods.}

\item{filter}{Optional precomputed filter path; recomputed when \code{NULL}. It
must be the explicit single-step filter, which is the discretization the
Theorem 3 recursions are exact for.}

\item{conditional}{Optional \code{aci_conditional_spec} selecting a
conditional ACI
reduction; see \code{\link[=aci_conditional]{aci_conditional()}}.}

\item{init}{Optional list with the initial hidden \code{mean} and \code{cov}.}

\item{regularize}{Covariance policy for this call; see \code{\link[=aci_filter]{aci_filter()}}. One
record covers the whole call, and is returned in \code{meta$regularization}.}

\item{force_validate}{\code{FALSE} (the default) lets a \code{filter} that
\code{\link[=aci_filter]{aci_filter()}} produced for this same run, unaltered since, skip per-step
re-validation, as in \code{\link[=aci_smoother]{aci_smoother()}}.}
}
\value{
An assimilation path of kind \code{"online"}, carrying \code{meta$lag}, the
per-anchor \code{meta$lag_effective}, \code{meta$saturated} and \code{meta$scheme}. Its
kind is what keeps it out of the places a complete smoother is required:
\code{lag_table(smoother = )} rejects it with \code{aci_error_dims}.
}
\description{
Generic reconstructing the hidden state at each time from the observed
record up to a fixed number of steps ahead of it. The closed-form method is
used for a \code{cgns_model}; a general \code{stochastic_model} is out of scope in
this release.
}
\details{
\code{lag} is the number of future observations each estimate may condition on:
the estimate at index \code{j} uses the observed record through index \code{j + lag},
and saturates at the end of the record. \code{lag = 0} returns the filter
moments unchanged. \code{lag = Inf} returns the complete Theorem 3 posterior
given the whole record.
}
\section{Methods (by class)}{
\itemize{
\item \code{aci_online(cgns_model)}: Closed-form fixed-lag online Theorem 3 smoother for a
conditional-Gaussian model.

\item \code{aci_online(stochastic_model)}: Classed not-implemented condition for a general
(non-CGNS) stochastic model.

}}
\section{Scheme}{

\code{aci_online()} computes the \strong{discrete} Theorem 3 posterior: the exact
conditional law of the hidden state given the observed increments on the
sampling grid under the explicit single-step discretization. \code{\link[=aci_smoother]{aci_smoother()}}
integrates the \strong{continuous} backward smoothing equations with an Euler
step of the same size. These are two discretizations of the same
continuous-time object and they agree only to first order in the step, so at
full lag \code{aci_online()} does \strong{not} reproduce \code{\link[=aci_smoother]{aci_smoother()}}. The gap
grows
with the length of the record; it is not a constant offset. On the packaged
ENSO partition (\code{l = 3}, \code{dt = 0.005}) the smoothed means differ by up to
1.89e-02 against a mean scale of 0.388 over 401 steps, and by up to 9.58e-02
against a scale of 2.22 over 4001 steps. The resulting ACI values differ by
up to 0.104 against a scale of 1.093 at 401 steps and 0.482 against 2.347 at
4001. \code{\link[=lag_table]{lag_table()}} and the causal influence range estimators use the
discrete scheme throughout, which is why \code{lt_diag()} and \code{aci()$aci} can
differ by that same amount. \code{meta$scheme} records which scheme produced a
path: \code{"theorem3_discrete"} here and on the lag table's reference smoother,
\code{"backward_ode_euler"} on \code{\link[=aci_smoother]{aci_smoother()}}.

The one exact boundary is the other end: at \code{lag = 0} the returned moments
are the filter moments, unchanged value for value.
}

\examples{
m <- aci_dyad_model()
sim <- simulate(m, seed = 1, t_end = 2, dt = 0.01)
ob <- as_obs(sim)
aci_online(m, ob, lag = 5)

}
\references{
Andreou, M., Chen, N. and Li, Y. (2026). An adaptive online smoother with
closed-form solutions and information-theoretic lag selection for
conditional Gaussian nonlinear systems. \emph{Journal of Nonlinear Science}
\strong{36}(4), 71. \doi{10.1007/s00332-026-10271-x}
}
\seealso{
\code{\link[=aci_filter]{aci_filter()}}, \code{\link[=aci_smoother]{aci_smoother()}}, \code{\link[=lag_table]{lag_table()}}
}
