% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-assimilation.R
\name{aci_filter}
\alias{aci_filter}
\alias{aci_filter.cgns_model}
\alias{aci_filter.stochastic_model}
\title{Data assimilation filter}
\usage{
aci_filter(model, obs, ...)

\method{aci_filter}{cgns_model}(
  model,
  obs,
  init = NULL,
  conditional = NULL,
  stepper = c("explicit", "implicit"),
  nsub = 1L,
  regularize = NULL,
  loglik = TRUE,
  ...
)

\method{aci_filter}{stochastic_model}(model, obs, ...)
}
\arguments{
\item{model}{A \code{cgns_model} or \code{stochastic_model} object.}

\item{obs}{An observed trajectory, or anything \code{\link[=as_obs]{as_obs()}} accepts.}

\item{...}{Arguments passed to methods.}

\item{init}{Optional list with the initial hidden \code{mean} and \code{cov}; \code{NULL}
uses a diffuse prior and warns.}

\item{conditional}{Optional \code{aci_conditional_spec} selecting a
conditional ACI
reduction; see \code{\link[=aci_conditional]{aci_conditional()}}.}

\item{stepper}{Either \code{"explicit"} or \code{"implicit"}; the implicit Riccati step
preserves positivity.}

\item{nsub}{Positive whole number of sub-steps taken per observation.}

\item{regularize}{Covariance policy for this call. \code{"none"} (the default,
and the value of \code{getOption("aci.regularize")} when it is unset) stops
with a classed \code{aci_error_covariance_not_spd} naming the site, grid index
and time as soon as a covariance leaves the positive-definite cone.
\code{"floor"} is the previous behaviour: the covariance is projected back by
\code{\link[=spd_floor]{spd_floor()}}, every such event is recorded in the result's
\code{meta$regularization}, and a call in which at least one floor fires raises
one \code{aci_warn_regularized} naming the first floored site, its grid index
and its time. Flooring changes the numerical covariance so that the
recursion can continue; it establishes nothing about the accuracy of the
reconstruction or of the resulting information score, and a large finite
ACI obtained after a floor is a diagnostic, not a result. A realised
observation-noise Gram whose reciprocal condition number falls below
1e-12 at an interval start raises \code{aci_error_gram_path}, a subclass of
\code{aci_error_gram}, naming the grid index, the time and the \code{rcond}; that
check runs before any covariance policy, so \code{regularize = "floor"} does
not bypass it. It is a conditioning test, not a noise-floor test:
\code{rcond()} is invariant under a uniform rescaling of the Gram, so for a
one-dimensional observed state, where the reciprocal condition number of
every non-zero Gram is exactly 1, only an exactly zero Gram is refused.
An observation noise that collapses towards zero without reaching it
passes this test and fails later as integration instability.
A mean or predictive log-likelihood that overflows to
\code{Inf} or \code{NaN} raises \code{aci_error_nonfinite} naming the quantity, the grid
index and the time.}

\item{loglik}{\code{TRUE} (the default) accumulates the predictive
log-likelihood into \code{meta$loglik}. \code{FALSE} skips that work; the filter
moments are unchanged and \code{meta$loglik} is \code{NULL}. The likelihood is not
used by ACI, so \code{FALSE} is the cheaper choice when only the state estimate
is wanted.}
}
\value{
An assimilation path: \code{da_path_gaussian} for the closed-form engine.
Its \code{meta$loglik} holds the predictive log-likelihood of the observed
record, or \code{NULL} when the method was called with \code{loglik = FALSE}.
}
\description{
Generic reconstructing the hidden state from the observed record up to each
time. The closed-form method is used for a \code{cgns_model}; a general
\code{stochastic_model} is out of scope in this release.
}
\section{Methods (by class)}{
\itemize{
\item \code{aci_filter(cgns_model)}: Closed-form filter for a conditional-Gaussian model.

\item \code{aci_filter(stochastic_model)}: Classed not-implemented condition for a general
(non-CGNS) stochastic model.

}}
\examples{
m <- aci_dyad_model()
sim <- simulate(m, seed = 1, t_end = 2, dt = 0.01)
ob <- as_obs(sim)
f <- aci_filter(m, ob)
f

}
\seealso{
\code{\link[=aci_smoother]{aci_smoother()}}, \code{\link[=aci]{aci()}}, \code{\link[=lag_table]{lag_table()}}
}
