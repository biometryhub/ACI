# The package is loaded by tests/testthat.R (and by testthat::test_local()).
# Do not source R/ here: installed-package checks run from a temporary library,
# where searching upward for DESCRIPTION is unnecessary and unreliable.
