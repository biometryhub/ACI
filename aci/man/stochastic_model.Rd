% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_classes.R
\name{stochastic_model}
\alias{stochastic_model}
\title{General stochastic model}
\usage{
stochastic_model(
  f,
  g,
  Sx,
  Sy,
  k,
  l,
  vectorized_members = FALSE,
  name = NULL,
  meta = list()
)
}
\arguments{
\item{f}{Function giving the observed drift at \verb{(t, x, y)}.}

\item{g}{Function giving the hidden drift at \verb{(t, x, y)}.}

\item{Sx}{Function giving the observed diffusion coefficient at \verb{(t, x)}.}

\item{Sy}{Function giving the hidden diffusion coefficient at \verb{(t, x, y)}.}

\item{k}{Observed dimension; a positive whole number.}

\item{l}{Hidden dimension; a positive whole number.}

\item{vectorized_members}{\code{TRUE} when the drifts accept a matrix of ensemble
members rather than a single hidden state.}

\item{name}{Optional 1-length character label for the model.}

\item{meta}{Optional named list of metadata carried on the object.}
}
\value{
An object of class \code{stochastic_model}.
}
\description{
Constructs the general (not necessarily conditional-Gaussian) stochastic
model consumed by the ensemble engine. The observed and hidden drifts and
diffusions are supplied as functions and validated on a small probe set
before the object is returned.
}
\examples{
stochastic_model(
  f = function(t, x, y) -x + y,
  g = function(t, x, y) -y,
  Sx = function(t, x) matrix(0.3, 1, 1),
  Sy = function(t, x, y) matrix(0.3, 1, 1),
  k = 1, l = 1)

}
\seealso{
\code{\link[=cgns_model]{cgns_model()}}, \code{\link[=enkbf]{enkbf()}}
}
