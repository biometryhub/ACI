% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assimilation.R, R/ensemble.R
\name{da_filter}
\alias{da_filter}
\alias{da_filter.cgns_model}
\alias{da_filter.stochastic_model}
\title{Data assimilation filter}
\usage{
da_filter(model, obs, ...)

\method{da_filter}{cgns_model}(
  model,
  obs,
  init = NULL,
  nontarget = NULL,
  stepper = c("explicit", "implicit"),
  nsub = 1L,
  ...
)

\method{da_filter}{stochastic_model}(
  model,
  obs,
  m = 100,
  localization = NULL,
  inflation = 1,
  seed = NULL,
  nontarget = NULL,
  ...
)
}
\arguments{
\item{model}{A \code{cgns_model} or \code{stochastic_model} object.}

\item{obs}{An observed trajectory, or anything \code{\link[=as_obs]{as_obs()}} accepts.}

\item{...}{Arguments passed to methods.}

\item{init}{Optional list with the initial hidden \code{mean} and \code{cov}; \code{NULL}
uses a diffuse prior and warns.}

\item{nontarget}{Optional \code{nontarget_spec} selecting a conditional ACI
reduction; see \code{\link[=nontarget]{nontarget()}}.}

\item{stepper}{Either \code{"explicit"} or \code{"implicit"}; the implicit Riccati step
preserves positivity.}

\item{nsub}{Positive whole number of sub-steps taken per observation.}

\item{m}{Ensemble size; a positive whole number.}

\item{localization}{Optional localization specification; see
\code{\link[=localization_spec]{localization_spec()}}.}

\item{inflation}{Positive multiplicative variance inflation factor.}

\item{seed}{Optional non-negative whole number seeding the generator.}
}
\value{
An assimilation path: \code{da_path_gaussian} for the closed-form engine,
\code{da_path_ensemble} for the ensemble engine.
}
\description{
Generic reconstructing the hidden state from the observed record up to each
time. The closed-form method is used for a \code{cgns_model}; a general
\code{stochastic_model} is filtered by the ensemble engine.
}
\section{Methods (by class)}{
\itemize{
\item \code{da_filter(cgns_model)}: Closed-form filter for a conditional-Gaussian model.

\item \code{da_filter(stochastic_model)}: Ensemble filter for a general stochastic model.

}}
\examples{
m <- model_dyad()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
ob <- as_obs(sim)
f <- da_filter(m, ob)
f

}
\seealso{
\code{\link[=da_smooth]{da_smooth()}}, \code{\link[=aci]{aci()}}, \code{\link[=lag_table]{lag_table()}}
}
