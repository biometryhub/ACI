% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extremes.R
\name{detect_events}
\alias{detect_events}
\title{Detect extreme events in an observed component (moser2026extremes s4.1-4.3
conventions)}
\usage{
detect_events(
  obs,
  component = 1,
  threshold = c("quantile", "event_peak_quantile", "absolute"),
  q = 0.9,
  value = NULL,
  two_sided = FALSE,
  min_separation = 0,
  burn_in = 0,
  event_unit = c("sign_excursion", "threshold_run")
)
}
\arguments{
\item{obs}{Observed trajectory.}

\item{component}{Observed component index.}

\item{threshold}{Threshold mode: a raw-sample empirical quantile,
a quantile of sign-excursion peak amplitudes, or an absolute magnitude.}

\item{q}{Quantile probability when \code{threshold = "quantile"} or
\code{"event_peak_quantile"}.}

\item{value}{Absolute threshold when \code{threshold = "absolute"}.}

\item{two_sided}{Whether to detect both upper and lower extremes.}

\item{min_separation}{Minimum separation between retained event peaks.}

\item{burn_in}{Ignore observations before this time.}

\item{event_unit}{Whether events are sign-consistent excursions or complete threshold runs.}
}
\value{
An \code{event_set} data frame with one row per event: \code{peak_index},
\code{t_star} and \code{peak_value} (the peak), \code{sign}, \code{run_start} and \code{run_end}
(the containing excursion or threshold run), and \code{duration}, the
half-peak width (moser2026extremes Appendix A feature 3). \code{duration}
measures the peak, not the run, so it is not \code{run_end - run_start}.
}
\description{
\code{threshold = "event_peak_quantile"} applies the quantile to amplitudes of
sign-consistent excursion peaks, matching the moser2026extremes dyad
convention. The raw trajectory quantile route (\code{threshold = "quantile"}) is
retained for the pathways-style workflow; the two definitions are not
interchangeable.
}
\examples{
m <- model_dyad()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
ob <- as_obs(sim)
detect_events(ob, q = 0.9)

}
\references{
Moser, C., Chen, N. and Andreou, M. (2026). Mechanisms and pathways of
extreme events in partially-observed stochastic dynamical systems.
arXiv:2605.22692. \doi{10.48550/arXiv.2605.22692}
}
