% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula_interface.R
\name{aci_fit}
\alias{aci_fit}
\title{Fit a latent-cause model from a formula}
\usage{
aci_fit(
  formula,
  data,
  time = NULL,
  group = NULL,
  degree = 2L,
  ce_threshold = "auto",
  energy_pairs = NULL,
  n_samples = 20,
  hidden_prior = list(decay = 0.7, sd = 1),
  stepper = c("explicit", "implicit"),
  nsub = 1L,
  seed = 1,
  ...
)
}
\arguments{
\item{formula}{A formula naming the observed variables on the left and the
latent variables on the right.}

\item{data}{A data frame, or a list of data frames, holding the observed
series.}

\item{time}{Optional name of the time column; \code{NULL} assumes a regular grid.}

\item{group}{Optional name of a grouping column splitting \code{data} into
segments.}

\item{degree}{Positive whole number, the polynomial degree of the candidate
library.}

\item{ce_threshold}{Either \code{"auto"} or a numeric causation entropy threshold.}

\item{energy_pairs}{Optional list of index pairs constrained to conserve
energy across equations.}

\item{n_samples}{Number of posterior path samples drawn when a latent
variable is present.}

\item{hidden_prior}{List with the prior \code{decay} and \code{sd} of the latent
process.}

\item{stepper}{Either \code{"explicit"} or \code{"implicit"}.}

\item{nsub}{Positive whole number of sub-steps taken per observation.}

\item{seed}{Non-negative whole number seeding the sampler.}

\item{...}{Passed to the partial-observation learner.}
}
\value{
An object of class \code{aci_fit}.
}
\description{
\code{aci_fit()} provides a modelling front end for a latent-cause formula.
Partial-observation learning is experimental and is labelled as such in
fitted objects and summaries.
}
\examples{
\donttest{
sim <- simulate(model_dyad(), seed = 8, T = 5, dt = 5e-3, burn_in = 1)
dat <- data.frame(t = sim$obs$t, x1 = sim$obs$x[, 1], y = sim$hidden[, 1])
aci_fit(y ~ x1, data = dat)
}

}
\seealso{
\code{\link[=cir]{cir()}}, \code{\link[=learn_model]{learn_model()}}, \code{\link[=aci]{aci()}}
}
