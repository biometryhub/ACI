% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_metrics.R
\name{cir_pair}
\alias{cir_pair}
\title{Forward and backward causal influence ranges together}
\usage{
cir_pair(model, obs, engine = "auto", nontarget = NULL, ...)
}
\arguments{
\item{model}{A \code{cgns_model} object; the closed-form engine is required.}

\item{obs}{An observed trajectory, or anything \code{\link[=as_obs]{as_obs()}} accepts.}

\item{engine}{One of \code{"auto"} or \code{"cgns"}.}

\item{nontarget}{Optional \code{nontarget_spec}; see \code{\link[=nontarget]{nontarget()}}.}

\item{...}{Passed to \code{\link[=aci]{aci()}}.}
}
\value{
An object of class \code{cir_pair}, a list with the \code{aci} result and the
\code{forward} and \code{backward} ranges.
}
\description{
Runs the metric once and returns the forward and backward ranges alongside
it, so that both directions share one filter pass and one prior.
}
\examples{
\donttest{
m <- model_dyad()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
ob <- as_obs(sim)
cir_pair(m, ob)
}

}
\seealso{
\code{\link[=forward_cir]{forward_cir()}}, \code{\link[=backward_cir]{backward_cir()}}
}
