% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assimilation.R
\name{lt_contraction_certificate}
\alias{lt_contraction_certificate}
\title{Per-step contraction certificate for the Theorem 3 update}
\usage{
lt_contraction_certificate(x)
}
\arguments{
\item{x}{A \code{lag_table} object.}
}
\value{
A data frame with one row per grid step, carrying \code{j}, \code{t},
\code{lambda_min}, \code{enorm} and \code{rho_E}, with attributes \code{gamma} (the largest
\code{enorm}) and \code{condition_318} (\code{TRUE} when every \code{lambda_min} is
positive).
}
\description{
Evaluates the checkable per-step contraction condition of the online
smoother update matrices (andreou2026smoother eqs. 3.18-3.19): when the
Hermitian part of \code{(I - E^j) / dt} is positive definite at every step,
the spectral radius of every \code{E^j} falls below one for a small enough
step. Reported per step: \code{lambda_min}, the smallest eigenvalue of that
Hermitian part (positive at the step means condition 3.18 holds there);
\code{enorm}, the operator 2-norm of \code{E^j}; and \code{rho_E}, its spectral radius.
}
\details{
The condition is stated for the continuous-time generator and holds for
\code{dt} sufficiently small, so a positive \code{lambda_min} alongside an \code{enorm}
at or above one flags a step-size margin rather than a broken model. The
spectral radius is not submultiplicative, so no bound on the accumulated
update products follows from \code{rho_E < 1} alone; \code{\link[=lt_tail_bound]{lt_tail_bound()}} remains
a heuristic estimate.
}
\examples{
m <- model_dyad()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
tb <- lag_table(m, as_obs(sim), mode = "forward",
                init = list(mean = 0, cov = diag(1, 1)))
cert <- lt_contraction_certificate(tb)
attr(cert, "gamma"); attr(cert, "condition_318")

}
\references{
Andreou, M., Chen, N. and Li, Y. (2026). An adaptive online smoother with
closed-form solutions and information-theoretic lag selection for
conditional Gaussian nonlinear systems. \emph{Journal of Nonlinear Science}
\strong{36}(4), 71. \doi{10.1007/s00332-026-10271-x}
}
\seealso{
\code{\link[=lag_table]{lag_table()}}, \code{\link[=lt_tail_bound]{lt_tail_bound()}}, \code{\link[=truncation_profile]{truncation_profile()}}
}
