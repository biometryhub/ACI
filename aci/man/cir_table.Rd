% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_metrics.R
\name{cir_table}
\alias{cir_table}
\title{Forward and backward causal influence ranges as one tidy frame}
\usage{
cir_table(
  model,
  obs,
  init = NULL,
  at = NULL,
  direction = c("forward", "backward", "both"),
  nontarget = NULL,
  tol = 1e-04,
  min_M = "auto",
  method = c("exact", "l1_linf"),
  max_lag = Inf,
  window = 3L,
  ...
)
}
\arguments{
\item{model}{A CGNS model.}

\item{obs}{Observed trajectory.}

\item{init}{Optional Gaussian initialization.}

\item{at}{Optional anchor times (used to select forward rows and/or backward
reference times).}

\item{direction}{Direction or directions to calculate.}

\item{nontarget}{Optional conditional ACI masking specification.}

\item{tol}{Adaptive-lag tolerance.}

\item{min_M}{Minimum discrepancy strength; \code{"auto"} uses the package
threshold option and masks low-signal ranges.}

\item{method}{Exact layer-cake objective or the \code{l1_linf} ratio.}

\item{max_lag}{Maximum number of positive-lag cells retained per forward row.}

\item{window}{Consecutive small-tail window used by adaptive storage.}

\item{...}{Unused arguments are rejected.}
}
\value{
A data frame with one row per anchor and direction, carrying \code{t},
\code{tau}, \code{strength} and \code{direction}; backward rows also carry
\code{above_baseline}, the \code{\link[=backward_cir]{backward_cir()}} validity gate (\code{NA} on forward
rows).
}
\description{
Wraps the lag table / CIR machinery and returns a data.frame with one row per
anchor: \code{t}, \code{tau} (the range, in the model's time units), \code{strength} (the
influence strength there) and \code{direction}. \code{nontarget} gives the conditional
range. Both directions receive \code{init}, so the backward pass runs from the same
prior as the forward one.
}
\examples{
\donttest{
m <- model_dyad()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
ob <- as_obs(sim)
head(cir_table(m, ob, direction = "forward"))
}

}
\seealso{
\code{\link[=forward_cir]{forward_cir()}}, \code{\link[=backward_cir]{backward_cir()}}, \code{\link[=cir_pair]{cir_pair()}}
}
