% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark_models.R
\name{model_multiscale_fbcir}
\alias{model_multiscale_fbcir}
\title{Four-dimensional multiscale atmospheric FBCIR benchmark}
\usage{
model_multiscale_fbcir(
  epsilon = 0.1,
  partition = c("joint", "y1", "y2"),
  params = NULL
)
}
\arguments{
\item{epsilon}{Positive fast/slow timescale parameter.  The paper uses 0.1.}

\item{partition}{One of \code{"joint"}, \code{"y1"}, or \code{"y2"}.}

\item{params}{Optional named parameter overrides.}
}
\value{
A \code{cgns_model} with the original shared Wiener channels preserved.
}
\description{
Constructs the joint CGNS split \verb{(y1,y2) -> (x1,x2)} or either conditional
marginal split \verb{y1 -> x1 | (x2,y2)} and \verb{y2 -> x2 | (x1,y1)}.  In the
marginal splits the conditioning variables remain in the observed state;
select the target channel recorded in \code{meta$target_obs_idx} when applying a
conditional likelihood mask.
}
\examples{
model_multiscale_fbcir(epsilon = 0.1)

}
