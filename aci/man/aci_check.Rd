% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_diagnostics.R
\name{aci_check}
\alias{aci_check}
\title{One-call diagnostic for a fitted or specified ACI model}
\usage{
aci_check(
  model,
  obs,
  init = NULL,
  test = TRUE,
  B = 99L,
  floor = 0.001,
  plot = TRUE,
  scale = 1,
  unit = "",
  seed = NULL,
  cir = TRUE,
  ...
)
}
\arguments{
\item{model}{A fitted or specified CGNS model.}

\item{obs}{An observed trajectory coercible with \code{as_obs()}.}

\item{init}{Optional Gaussian initialization list with \code{mean} and \code{cov}.}

\item{test}{Whether to run the surrogate-null diagnostic.}

\item{B}{Number of bootstrap replicates.}

\item{floor}{Numerical floor used by the effect-size screen.}

\item{plot}{Whether to draw diagnostic plots.}

\item{scale}{Multiplicative scale for the displayed hidden state.}

\item{unit}{Label for the hidden-state scale.}

\item{seed}{Reproducibility seed.}

\item{cir}{Whether to calculate CIR summaries.}

\item{...}{Additional arguments forwarded to lower-level routines.}
}
\description{
The counterpart to \code{gam.check()}: draws the reconstructed latent with its
uncertainty band and the influence metric, then prints the verdict, the
forward CIR, and a reconstruction-consistency check. The latter tests the
full matrix ordering \verb{filter covariance - smoother covariance >= 0} at every
step, not only the first latent marginal. The compact plot still shows latent
component 1; the returned consistency diagnostics cover every component.
\code{test = TRUE} runs the surrogate-null likelihood-ratio bootstrap; \code{FALSE}
reports the effect-size screen only.
}
\examples{
\donttest{
m <- model_dyad()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
ob <- as_obs(sim)
aci_check(m, ob, test = FALSE, plot = FALSE)
}

}
