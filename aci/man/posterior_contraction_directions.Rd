% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extremes.R
\name{posterior_contraction_directions}
\alias{posterior_contraction_directions}
\title{Filter-to-smoother uncertainty-contraction directions (package diagnostic,
not the moser2026extremes event-conditioned sensitive-direction estimand).}
\usage{
posterior_contraction_directions(paths, at)
}
\arguments{
\item{paths}{List containing compatible Gaussian \code{filter} and \code{smoother} paths.}

\item{at}{Absolute time or times at which to calculate directions.}
}
\value{
A list of generalized eigen-decompositions, one per requested time,
or that list's single element when one time is requested.
}
\description{
Filter-to-smoother uncertainty-contraction directions (package diagnostic,
not the moser2026extremes event-conditioned sensitive-direction estimand).
}
\examples{
m <- model_dyad()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
ob <- as_obs(sim)
f <- da_filter(m, ob)
s <- da_smooth(m, ob, filter = f)
posterior_contraction_directions(list(filter = f, smoother = s), at = 1)

}
\references{
Moser, C., Chen, N. and Andreou, M. (2026). Mechanisms and pathways of
extreme events in partially-observed stochastic dynamical systems.
arXiv:2605.22692. \doi{10.48550/arXiv.2605.22692}
}
\seealso{
\code{\link[=sensitive_directions]{sensitive_directions()}}, \code{\link[=event_stats]{event_stats()}}
}
