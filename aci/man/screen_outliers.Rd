% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/applied_workflows.R
\name{screen_outliers}
\alias{screen_outliers}
\title{Screen one-sided dropouts from an irregular observation series}
\usage{
screen_outliers(
  t,
  value,
  lo = -Inf,
  hi = Inf,
  k = 3,
  floor_abs = 0.1,
  iter = 3L,
  df_max = 5
)
}
\arguments{
\item{t}{Observation times.}

\item{value}{Numeric observation series.}

\item{lo, hi}{Physical lower and upper bounds.}

\item{k}{Robust residual cutoff multiplier.}

\item{floor_abs}{Minimum absolute residual cutoff.}

\item{iter}{Number of screen/refit iterations.}

\item{df_max}{Maximum screening-spline degrees of freedom.}
}
\description{
Rejects points that fall implausibly far BELOW a deliberately stiff fit, plus
anything outside the physical range \verb{[lo, hi]}. The stiffness matters: left to
generalised cross-validation a spline with more than a dozen dates interpolates
straight through the dropouts, residuals stay small and nothing is rejected.
\code{df_max} therefore caps the flexibility of the screening fit only.
}
\details{
Returns a logical vector over the input, with the fitted values attached.
}
\examples{
set.seed(1)
tt <- seq(0, 10, length.out = 30)
v <- sin(tt) + rnorm(30, sd = 0.05)
v[c(5, 20)] <- v[c(5, 20)] - 1
screen_outliers(tt, v)

}
