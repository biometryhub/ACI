% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_classes.R
\name{cgns_from_affine}
\alias{cgns_from_affine}
\title{Build cgns_model  from a general affine-in-hidden drift pair by exact}
\usage{
cgns_from_affine(
  f_full,
  g_full,
  Sx,
  Sy_hidden,
  k,
  l,
  name = NULL,
  Sx2 = NULL,
  Sy_shared = NULL,
  meta = list()
)
}
\arguments{
\item{f_full}{Function of \verb{(t, x, y)} giving the full observed drift.}

\item{g_full}{Function of \verb{(t, x, y)} giving the full hidden drift.}

\item{Sx}{Function of \verb{(t, x)} giving the observed diffusion on the first
Wiener channel.}

\item{Sy_hidden}{Function of \verb{(t, x)} giving the hidden diffusion on its own
Wiener channel.}

\item{k}{Observed dimension; a positive whole number.}

\item{l}{Hidden dimension; a positive whole number.}

\item{name}{Optional 1-length character label for the model.}

\item{Sx2}{Optional function of \verb{(t, x)} giving the observed diffusion on the
second Wiener channel.}

\item{Sy_shared}{Optional function of \verb{(t, x)} giving the hidden diffusion on
the shared Wiener channel, which introduces correlated noise.}

\item{meta}{Optional named list of metadata carried on the object.}
}
\value{
An object of class \code{cgns_model}.
}
\description{
Recovers the conditional-Gaussian coefficients from drift functions written
in terms of the full state, by exact affine differencing. The supplied
functions are checked before they are replaced by the reconstructed
representation, so a drift that is not affine in the hidden state is
rejected rather than silently linearised.
}
\examples{
cgns_from_affine(
  f_full = function(t, x, y) -0.5 * x + y,
  g_full = function(t, x, y) -0.5 * y,
  Sx = function(t, x) matrix(0.5, 1, 1),
  Sy_hidden = function(t, x) matrix(1, 1, 1),
  k = 1, l = 1)

}
\seealso{
\code{\link[=cgns_model]{cgns_model()}}
}
