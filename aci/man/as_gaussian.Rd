% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble.R
\name{as_gaussian}
\alias{as_gaussian}
\title{Gaussian moments of an ensemble path}
\usage{
as_gaussian(path)
}
\arguments{
\item{path}{A \code{da_path_ensemble} object.}
}
\value{
A \code{da_path_gaussian} object carrying the ensemble moments.
}
\description{
Reduces an ensemble path to its per-time sample mean and covariance, giving
an object the closed-form diagnostics can consume.
}
\examples{
m <- model_dyad()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
ob <- as_obs(sim)
fr <- enkbf(m, ob, m = 20, seed = 1)
as_gaussian(fr$path)

}
\seealso{
\code{\link[=enkbf]{enkbf()}}, \code{\link[=cross_validate]{cross_validate()}}
}
