% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_metrics.R
\name{backward_cir}
\alias{backward_cir}
\alias{backward_cir.lag_table}
\alias{backward_cir.aci_result}
\alias{backward_cir.cgns_model}
\title{Backward causal influence range}
\usage{
backward_cir(x, ...)

\method{backward_cir}{lag_table}(
  x,
  method = c("exact", "l1_linf"),
  eps = NULL,
  min_M = "auto",
  masked_value = c("na", "zero"),
  quadrature = c("sum", "simpson"),
  simpson_close = c("quadratic", "trapezoid"),
  ...
)

\method{backward_cir}{aci_result}(x, T = "end", method = "exact", ...)

\method{backward_cir}{cgns_model}(x, obs, T, nontarget = NULL, init = NULL, ...)
}
\arguments{
\item{x}{A \code{lag_table}, \code{aci_result} or \code{cgns_model} object.}

\item{...}{Arguments passed to methods.}

\item{method}{Either \code{"exact"} or \code{"l1_linf"}.}

\item{eps}{Optional numeric vector of finite non-negative thresholds at which
the subjective range is also reported.}

\item{min_M}{Either \code{"auto"} or one finite non-negative number; a range whose
strength falls below it is masked.}

\item{masked_value}{What a masked range reports: \code{"na"} (the default) keeps
the masking visible as \code{NA}; \code{"zero"} follows andreou2026cir Remarks B.4
and C.4 and the FBCIR scripts, which set the length to 0.}

\item{quadrature}{Quadrature for the \code{l1_linf} ratio: \code{"sum"} (the default)
is the literal andreou2026cir eq. G.14 L1 grid-function sum, the FBCIR
scripts' active line; \code{"simpson"} is the smoother alternative those
scripts retain in comments. The choice is recorded in the result's
\code{meta$quadrature}; the \code{exact} form always integrates with Simpson.}

\item{simpson_close}{Closing rule for the leftover interval when a grid has
an even number of points: \code{"quadratic"} (the default) fits a quadratic
through the last three points, following \code{simps.m} in the ACI reference
code; \code{"trapezoid"} is the package's pre-0.0.21 rule and reproduces
results reported by earlier versions. Odd-length grids are unaffected.}

\item{T}{Either \code{"end"} or numeric reference times within the observed grid.}

\item{obs}{An observed trajectory, or anything \code{\link[=as_obs]{as_obs()}} accepts.}

\item{nontarget}{Optional \code{nontarget_spec}; see \code{\link[=nontarget]{nontarget()}}.}

\item{init}{Optional list with the initial hidden \code{mean} and \code{cov}.}
}
\value{
An object of class \code{cir_result} with \code{direction} \code{"backward"}.
}
\description{
Summarizes the duration of influence on the discrete time grid, backward from
a reference time. A backward result is anchored at \code{T}, but its computable
lagged grid and physical interval end at \code{T - dt}; its bound label records
this convention. The backward \code{l1_linf} ratio is evaluated with the plain
Appendix G L1 sum, following the FBCIR code's active line; the exact form
integrates its suffix minima with composite Simpson.
}
\details{
A backward result also reports the andreou2026cir Section 2.3.4 validity
gate in its \code{meta}: \code{baseline} (the initial-time information deficit, the
one-lag metric at t = 0), \code{terminal} (the metric at the \code{T - dt} end of the
grid), and \code{above_baseline} (their comparison, eq. 21 and Remark C.2).
A large \code{M} with \code{above_baseline = FALSE} means the attribution evidence
never rises above the initial-time baseline; treat such a range with
caution rather than as a long attribution window.
}
\section{Methods (by class)}{
\itemize{
\item \code{backward_cir(lag_table)}: Backward range from a one-lag table.

\item \code{backward_cir(aci_result)}: Backward range from an ACI result, at one or more
reference times.

\item \code{backward_cir(cgns_model)}: Backward range straight from a model and its
observations, without first assembling an ACI result.

}}
\examples{
m <- model_dyad()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
ob <- as_obs(sim)
tb <- lag_table(m, ob, mode = "one_lag")
backward_cir(tb)

}
\references{
Andreou, M. and Chen, N. (2026). Bridging prediction and attribution:
identifying forward and backward causal influence ranges using assimilative
causal inference. arXiv:2510.21889v2, 4 August 2026.
\doi{10.48550/arXiv.2510.21889}
}
\seealso{
\code{\link[=forward_cir]{forward_cir()}}, \code{\link[=lag_table]{lag_table()}}, \code{\link[=cir_pair]{cir_pair()}}
}
