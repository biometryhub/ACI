% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery.R
\name{sample_paths}
\alias{sample_paths}
\title{Sample hidden trajectories conditioned on an observed path}
\usage{
sample_paths(
  model,
  obs,
  n_samples = 20,
  method = c("ffbs", "enkbs"),
  seed = NULL,
  filter = NULL,
  nontarget = NULL,
  init = NULL,
  stepper = c("explicit", "implicit"),
  nsub = 1L,
  ...
)
}
\arguments{
\item{model}{Model used for assimilation.}

\item{obs}{Observed trajectory.}

\item{n_samples}{Number of hidden paths.}

\item{method}{FFBS or EnKBS sampler.}

\item{seed}{Reproducibility seed.}

\item{filter}{Optional compatible precomputed filter.}

\item{nontarget}{Optional conditional ACI masking specification.}

\item{init}{Optional Gaussian initial distribution. For FFBS this is passed
to the closed-form filter; for EnKBS it initializes the ensemble sampler.}

\item{stepper}{Filter covariance stepper used by FFBS.}

\item{nsub}{Number of filter substeps.}

\item{...}{Additional filter or ensemble arguments.}
}
\description{
Method \code{"ffbs"} uses the one-step Euler CGNS transition conditional on each
observed increment. Method \code{"enkbs"} returns EnKBS smoother members for a
compatible stochastic model.
}
\details{
The FFBS pass conditions on a filter it computes itself when \code{filter} is not
supplied. The transition below is the single-step Euler CGNS transition, so
FFBS requires the matching explicit, one-step filter. It does not silently mix
this transition with an implicit or sub-stepped filter.
}
\examples{
m <- model_dyad()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
ob <- as_obs(sim)
sample_paths(m, ob, n_samples = 3, seed = 1)

}
