% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_diagnostics.R
\name{cross_validate}
\alias{cross_validate}
\title{Cross-engine validation of ensemble against closed-form moments}
\usage{
cross_validate(
  model,
  obs,
  m_grid = c(10, 20, 50, 100),
  n_rep = 3,
  localization = NULL,
  inflation = 1,
  seed = 1,
  init = NULL,
  ...
)
}
\arguments{
\item{model}{A \code{cgns_model} object.}

\item{obs}{An observed trajectory, or anything \code{\link[=as_obs]{as_obs()}} accepts.}

\item{m_grid}{Numeric vector of ensemble sizes to compare.}

\item{n_rep}{Number of repetitions at each ensemble size.}

\item{localization}{Optional localization specification; see
\code{\link[=localization_spec]{localization_spec()}}.}

\item{inflation}{Positive multiplicative variance inflation factor.}

\item{seed}{Non-negative whole number seeding the ensembles.}

\item{init}{Optional list with the initial hidden \code{mean} and \code{cov}.}

\item{...}{Must be empty; unused arguments are an error.}
}
\value{
A data frame with one row per ensemble size and repetition, carrying
the discrepancy against the closed-form moments.
}
\description{
\code{cross_validate()} compares ensemble moments with a compatible closed-form
CGNS result. This is a diagnostic, not a substitute for checking model
assumptions or source provenance.
}
\examples{
\donttest{
m <- model_dyad()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
ob <- as_obs(sim)
cross_validate(m, ob, m_grid = c(10, 20), n_rep = 1)
}

}
\seealso{
\code{\link[=nil_causality_check]{nil_causality_check()}}, \code{\link[=as_gaussian]{as_gaussian()}}
}
