% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extremes.R
\name{event_stats}
\alias{event_stats}
\title{Composite conditional (event-locked) hidden statistics vs climatology}
\usage{
event_stats(paths, events, w_pre = 1.5, w_post = 1.5)
}
\arguments{
\item{paths}{A smoother path or list containing one.}

\item{events}{Event set from \code{detect_events()}.}

\item{w_pre}{Pre-event composite width.}

\item{w_post}{Post-event composite width.}
}
\description{
Composite conditional (event-locked) hidden statistics vs climatology
}
\examples{
\donttest{
m <- model_dyad()
sim <- simulate(m, seed = 1, T = 30, dt = 0.01)
ob <- as_obs(sim)
ev <- detect_events(ob, q = 0.95, min_separation = 4, burn_in = 2)
f <- da_filter(m, ob)
s <- da_smooth(m, ob, filter = f)
event_stats(list(filter = f, smoother = s), ev)
}

}
