% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/applied_workflows.R
\name{as_uniform_trajectory}
\alias{as_uniform_trajectory}
\title{Resample irregular observation channels onto the uniform grid the engine needs}
\usage{
as_uniform_trajectory(t, channels, dt, keep = NULL, df_max = Inf, floor = NULL)
}
\arguments{
\item{t}{Observation times.}

\item{channels}{Named numeric observation channels sharing \code{t}.}

\item{dt}{Requested uniform-grid spacing.}

\item{keep}{Optional logical row selector applied to all channels.}

\item{df_max}{Maximum smoothing-spline degrees of freedom.}

\item{floor}{Optional lower floor applied after interpolation.}
}
\description{
\code{channels} is a named list of numeric vectors sharing the times \code{t}. Each is
smoothed and evaluated on a uniform grid of step \code{dt}, and returned as an
\code{obs_traj}. \code{keep} (e.g. from \code{screen_outliers}) subsets the input first, so a
dropout screened on one channel removes that DATE from every channel.
}
\examples{
set.seed(1)
tt <- sort(runif(30, 0, 10))
as_uniform_trajectory(tt, list(ndvi = sin(tt)), dt = 0.5)

}
