% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assimilation.R
\name{reduce_nontarget}
\alias{reduce_nontarget}
\title{Reduce a model by prescribing the non-target channels}
\usage{
reduce_nontarget(model, obs, spec)
}
\arguments{
\item{model}{A \code{cgns_model} object.}

\item{obs}{An observed trajectory, or anything \code{\link[=as_obs]{as_obs()}} accepts.}

\item{spec}{A \code{nontarget_spec} object; see \code{\link[=nontarget]{nontarget()}}.}
}
\value{
A list with the reduced \code{model} and the reduced \code{obs}.
}
\description{
Rewrites a model so that the non-target observed channels enter as known
forcing rather than as observations to be assimilated, leaving the target
channels as the only observed process.
x_B becomes prescribed forcing in the (x_A, y) system.
}
\examples{
m2 <- cgns_model(
  Lx = function(t, x) matrix(c(1, 0), 2, 1),
  fx = function(t, x) -0.5 * x,
  Ly = function(t, x) matrix(-0.5, 1, 1),
  fy = function(t, x) 0,
  Sx1 = function(t, x) diag(0.5, 2),
  Sy2 = function(t, x) matrix(1, 1, 1),
  k = 2, l = 1)
sim2 <- simulate(m2, seed = 1, T = 1, dt = 0.01)
reduce_nontarget(m2, as_obs(sim2), nontarget(2))

}
\seealso{
\code{\link[=nontarget]{nontarget()}}
}
