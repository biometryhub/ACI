% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assimilation.R, R/ensemble.R
\name{da_smooth}
\alias{da_smooth}
\alias{da_smooth.cgns_model}
\alias{da_smooth.stochastic_model}
\title{Data assimilation smoother}
\usage{
da_smooth(model, obs, ...)

\method{da_smooth}{cgns_model}(
  model,
  obs,
  filter = NULL,
  nontarget = NULL,
  init = NULL,
  stepper = c("explicit", "implicit"),
  nsub = 1L,
  ...
)

\method{da_smooth}{stochastic_model}(
  model,
  obs,
  filter = NULL,
  m = 100,
  localization = NULL,
  inflation = 1,
  seed = NULL,
  nontarget = NULL,
  ic_sampler = NULL,
  noise = NULL,
  ...
)
}
\arguments{
\item{model}{A \code{cgns_model} or \code{stochastic_model} object.}

\item{obs}{An observed trajectory, or anything \code{\link[=as_obs]{as_obs()}} accepts.}

\item{...}{Arguments passed to methods.}

\item{filter}{Optional precomputed filter path; recomputed when \code{NULL}.}

\item{nontarget}{Optional \code{nontarget_spec} selecting a conditional ACI
reduction; see \code{\link[=nontarget]{nontarget()}}.}

\item{init}{Optional list with the initial hidden \code{mean} and \code{cov}.}

\item{stepper}{Either \code{"explicit"} or \code{"implicit"}.}

\item{nsub}{Positive whole number of sub-steps taken per observation.}

\item{m}{Ensemble size; a positive whole number.}

\item{localization}{Optional localization specification; see
\code{\link[=localization_spec]{localization_spec()}}.}

\item{inflation}{Positive multiplicative variance inflation factor.}

\item{seed}{Optional non-negative whole number seeding the generator.}

\item{ic_sampler}{Optional function of the ensemble size drawing the
initial members, or a list with the initial \code{mean} and \code{cov}; \code{NULL}
uses the model's default.}

\item{noise}{Optional stored Wiener increments of the forward run.}
}
\value{
An assimilation path: \code{da_path_gaussian} for the closed-form engine,
\code{da_path_ensemble} for the ensemble engine.
}
\description{
Generic reconstructing the hidden state from the whole observed record. The
closed-form method is used for a \code{cgns_model}; a general \code{stochastic_model}
is smoothed by the ensemble engine.
}
\section{Methods (by class)}{
\itemize{
\item \code{da_smooth(cgns_model)}: Closed-form backward-ODE smoother for a
conditional-Gaussian model.

\item \code{da_smooth(stochastic_model)}: Ensemble smoother for a general stochastic model.

}}
\examples{
m <- model_dyad()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
ob <- as_obs(sim)
f <- da_filter(m, ob)
da_smooth(m, ob, filter = f)

}
\seealso{
\code{\link[=da_filter]{da_filter()}}, \code{\link[=aci]{aci()}}
}
