% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/applied_workflows.R
\name{calibrate_cgns}
\alias{calibrate_cgns}
\title{Calibrate a CGNS model's rate parameters against an observed trajectory}
\usage{
calibrate_cgns(
  build,
  obs,
  start,
  bounds,
  y0,
  weights = NULL,
  restarts = c(1, 2, 0.5),
  sigma_floor = 0.01,
  maxit = 4000L
)
}
\arguments{
\item{build}{Function mapping a named parameter vector to a CGNS model.}

\item{obs}{Observed trajectory.}

\item{start}{Named starting parameter values.}

\item{bounds}{Named lower/upper bounds for fitted parameters.}

\item{y0}{Initial hidden state used by deterministic integration.}

\item{weights}{Optional named observation-channel weights.}

\item{restarts}{Number of bounded optimization starts.}

\item{sigma_floor}{Minimum inferred process-noise amplitude.}

\item{maxit}{Maximum optimizer iterations.}
}
\description{
Bounded, multi-start least squares of the model's deterministic solution
against the observed channels. \code{build(par)} returns a \code{cgns_model}; only the
names in \code{bounds} are fitted, everything else in \code{start} is held fixed, so
the cause's amplitude (the pool at sowing, the dated inputs) stays pinned to
the management record rather than being absorbed into the fit.
}
\details{
Bounds are not cosmetic. Left unconstrained a canopy series will drive the
pool-loss rate to zero, and a non-decaying pool violates the finite-memory
premise the CIR rests on: attribution windows then reach the start of the
record at every reference time. Parameters resting on a bound are reported in
\code{on_bound}, because they are not identified by the data.

Process-noise amplitudes are estimated from the increment residuals about the
calibrated deterministic path and floored, so the observed-channel Gram stays
well conditioned for the Riccati step.
}
\examples{
\donttest{
m <- model_dyad()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
ob <- as_obs(sim)
build <- function(p) model_dyad(params = list(
  d_x = p[["d_x"]], gamma = 2, f_x = 0.5, s_x = 0.5,
  d_y = 0.5, f_y = 1, s_y = 1))
calibrate_cgns(build, ob, start = c(d_x = 0.4),
               bounds = list(d_x = c(0.1, 1)), y0 = 2, restarts = 1, maxit = 5)
}

}
