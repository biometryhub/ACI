% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble.R
\name{enkbf}
\alias{enkbf}
\title{Ensemble Kalman-Bucy filter}
\usage{
enkbf(
  model,
  obs,
  m,
  ic_sampler = NULL,
  localization = NULL,
  inflation = 1,
  nontarget = NULL,
  seed = NULL,
  noise = NULL,
  keep_members = TRUE,
  ...
)
}
\arguments{
\item{model}{A \code{stochastic_model} object.}

\item{obs}{An observed trajectory, or anything \code{\link[=as_obs]{as_obs()}} accepts.}

\item{m}{Ensemble size; a positive whole number.}

\item{ic_sampler}{Optional function of the ensemble size drawing the initial
members, or a list with the initial \code{mean} and \code{cov} from which members
are drawn; \code{NULL} uses the model's default.}

\item{localization}{Optional localization specification; see
\code{\link[=localization_spec]{localization_spec()}}.}

\item{inflation}{Positive multiplicative variance inflation factor.}

\item{nontarget}{Optional \code{nontarget_spec}; see \code{\link[=nontarget]{nontarget()}}.}

\item{seed}{Optional non-negative whole number seeding the generator.}

\item{noise}{Optional stored Wiener increments to drive the run.}

\item{keep_members}{\code{TRUE} to retain the member paths.}

\item{...}{Must be empty; unused arguments are an error.}
}
\value{
A \code{da_path_ensemble} object of kind \code{"filter"}, carrying the Wiener
increments \code{\link[=enkbs]{enkbs()}} requires.
}
\description{
Ensemble Kalman-Bucy filtering following the independent-noise jiang2026enkbs
recursion. Shared/correlated observation and signal noise is not accepted by
this engine; use the closed-form CGNS engine for that case.
}
\examples{
m <- model_dyad()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
ob <- as_obs(sim)
enkbf(m, ob, m = 20, seed = 1)

}
\references{
Jiang, Z., Andreou, M., Reich, S. and Chen, N. (2026). A continuous-time
ensemble Kalman-Bucy smoother for causal inference and model discovery.
arXiv:2604.25157. \doi{10.48550/arXiv.2604.25157}
}
\seealso{
\code{\link[=enkbs]{enkbs()}}, \code{\link[=as_gaussian]{as_gaussian()}}, \code{\link[=localization_spec]{localization_spec()}}
}
