% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_diagnostics.R
\name{nil_causality_check}
\alias{nil_causality_check}
\title{Structural zero-coupling check}
\usage{
nil_causality_check(
  model,
  obs = NULL,
  direction,
  floor = 1e-04,
  T = 50,
  dt = 0.001,
  seed = 1
)
}
\arguments{
\item{model}{A \code{cgns_model} object.}

\item{obs}{Optional observed trajectory; \code{NULL} simulates one.}

\item{direction}{Character naming the causal direction to check.}

\item{floor}{Positive numeric level below which the metric counts as nil.}

\item{T}{Positive total simulated time used when \code{obs} is \code{NULL}.}

\item{dt}{Positive integration step used when \code{obs} is \code{NULL}.}

\item{seed}{Non-negative whole number seeding the simulation.}
}
\value{
A list carrying the realised metric and the verdict of the check.
}
\description{
\code{nil_causality_check()} is a structural zero-coupling check. It is a
diagnostic, not a substitute for checking model assumptions or source
provenance.
}
\examples{
nil_causality_check(model_dyad(), direction = list(cause = 1, effect = 1),
                    T = 5, dt = 0.01)

}
\seealso{
\code{\link[=nil_surrogate_test]{nil_surrogate_test()}}, \code{\link[=cross_validate]{cross_validate()}}
}
