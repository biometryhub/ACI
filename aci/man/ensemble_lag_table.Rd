% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble.R
\name{ensemble_lag_table}
\alias{ensemble_lag_table}
\title{Forward CIR ensemble lag table}
\usage{
ensemble_lag_table(
  model,
  obs,
  m = 100,
  seed = NULL,
  nontarget = NULL,
  localization = NULL,
  inflation = 1,
  ic_sampler = NULL,
  ...
)
}
\arguments{
\item{model}{A \code{stochastic_model} object.}

\item{obs}{An observed trajectory, or anything \code{\link[=as_obs]{as_obs()}} accepts.}

\item{m}{Ensemble size; must exceed the hidden dimension.}

\item{seed}{Optional non-negative whole number seeding the generator.}

\item{nontarget}{Optional \code{nontarget_spec}; see \code{\link[=nontarget]{nontarget()}}.}

\item{localization}{Optional localization specification; see
\code{\link[=localization_spec]{localization_spec()}}.}

\item{inflation}{Positive multiplicative variance inflation factor.}

\item{ic_sampler}{Optional function of the ensemble size drawing the
initial members, or a list with the initial \code{mean} and \code{cov}; \code{NULL}
uses the model's default.}

\item{...}{Must be empty; unused arguments are an error.}
}
\value{
An object of class \code{lag_table} built by the ensemble engine.
}
\description{
Ensemble lag table for forward causal influence ranges.
Evaluates complete-versus-lagged smoother divergence using EnKBF/EnKBS
posterior approximation. \code{ensemble_lag_table()} constructs andreou2026cir's
forward finite-lag posterior family by repeating the jiang2026enkbs backward
pass at every observation horizon. It has quadratic time-point work/storage
and uses full-dimensional Gaussian moment KL, so \code{m} must exceed the hidden
dimension.
}
\details{
This route is graded at machine precision against a literal R port of the
published jiang2026enkbs dyad experiment, driven by identical increments
(see \code{tests/testthat/helper-golden-p3.R}).
}
\examples{
\donttest{
m <- model_dyad()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
ob <- as_obs(sim)
ensemble_lag_table(m, ob, m = 20, seed = 1)
}

}
\references{
Andreou, M. and Chen, N. (2026). Bridging prediction and attribution:
identifying forward and backward causal influence ranges using assimilative
causal inference. arXiv:2510.21889v2, 4 August 2026.
\doi{10.48550/arXiv.2510.21889}

Jiang, Z., Andreou, M., Reich, S. and Chen, N. (2026). A continuous-time
ensemble Kalman-Bucy smoother for causal inference and model discovery.
arXiv:2604.25157. \doi{10.48550/arXiv.2604.25157}
}
\seealso{
\code{\link[=enkbs]{enkbs()}}, \code{\link[=forward_cir]{forward_cir()}}
}
