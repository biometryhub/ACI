% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery.R
\name{learn_model}
\alias{learn_model}
\title{Model discovery from observations + posterior hidden samples}
\usage{
learn_model(
  model,
  obs,
  library,
  n_samples = 20,
  method = c("ffbs", "enkbs"),
  ce_threshold = "auto",
  constraints = NULL,
  energy_pairs = NULL,
  enforce_cgns = FALSE,
  n_iter = 1,
  seed = 1,
  burn_frac = 0.1,
  ...
)
}
\arguments{
\item{model}{Seed model.}

\item{obs}{Observed trajectory.}

\item{library}{Function library specification.}

\item{n_samples}{Number of posterior hidden paths.}

\item{method}{Posterior sampling method.}

\item{ce_threshold}{Causation entropy selection threshold.}

\item{constraints}{Optional equality constraints.}

\item{energy_pairs}{Optional energy-conserving equation pairs.}

\item{enforce_cgns}{Restrict selected terms to the CGNS class.}

\item{n_iter}{Number of stochastic-EM iterations.}

\item{seed}{Reproducibility seed.}

\item{burn_frac}{Fraction discarded before fitting.}

\item{...}{Additional arguments passed to path sampling.}
}
\description{
jiang2026enkbs s5 loop (sample -> CE prune -> constrained MLE), iterated as
a stochastic-EM.
n_iter = 1 (default) is a single stochastic-EM M-step: structure recovery
is reliable, but hidden equation coefficients carry the posterior path
h-transform drift bias (~20-40\% on quadratic terms in the dyad demo).
n_iter >= 2 re-samples under the learned model (structure frozen after
the first pass); this loop is EXPERIMENTAL - without Robbins-Monro
damping it can amplify rather than remove the bias. Validation required against MATLAB Codebase.
}
\examples{
\donttest{
m <- model_dyad()
sim <- simulate(m, seed = 1, T = 2, dt = 0.01)
ob <- as_obs(sim)
lib <- cgns_library(k = 1, l = 1, degree = 2)
learn_model(m, ob, lib, n_samples = 3, seed = 1)
}

}
\references{
Jiang, Z., Andreou, M., Reich, S. and Chen, N. (2026). A continuous-time
ensemble Kalman-Bucy smoother for causal inference and model discovery.
arXiv:2604.25157. \doi{10.48550/arXiv.2604.25157}

Chen, N. and Zhang, Y. (2023). A causality-based learning approach for
discovering the underlying dynamics of complex systems from partial
observations with stochastic parameterization. \emph{Physica D} \strong{449}, 133743.
\doi{10.1016/j.physd.2023.133743}
}
