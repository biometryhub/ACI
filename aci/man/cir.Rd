% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula_interface.R
\name{cir}
\alias{cir}
\alias{cir.default}
\alias{cir.aci_fit}
\title{Causal influence range of a fitted object}
\usage{
cir(object, ...)

\method{cir}{default}(object, ...)

\method{cir}{aci_fit}(
  object,
  direction = c("forward", "backward"),
  at = NULL,
  tol = 0.001,
  full = FALSE,
  ...
)
}
\arguments{
\item{object}{A fitted object, currently an \code{aci_fit}.}

\item{...}{Arguments passed to methods.}

\item{direction}{Either \code{"forward"} or \code{"backward"}.}

\item{at}{Optional numeric anchor times; \code{NULL} uses the fitted grid.}

\item{tol}{Positive tolerance for the table's adaptive freeze rule.}

\item{full}{\code{TRUE} to return the whole tidy table rather than the range
object.}
}
\value{
An object of class \code{cir_result}, or a data frame when the full table
is requested.
}
\description{
\code{cir()} extracts a forward or backward causal influence range from a fitted
object.
}
\section{Methods (by class)}{
\itemize{
\item \code{cir(default)}: Errors for objects with no causal influence range method.

\item \code{cir(aci_fit)}: Causal influence range of a fitted latent-cause model.

}}
\examples{
\donttest{
sim <- simulate(model_dyad(), seed = 8, T = 5, dt = 5e-3, burn_in = 1)
dat <- data.frame(t = sim$obs$t, x1 = sim$obs$x[, 1], y = sim$hidden[, 1])
fit <- aci_fit(y ~ x1, data = dat)
cir(fit)
}

}
\seealso{
\code{\link[=forward_cir]{forward_cir()}}, \code{\link[=backward_cir]{backward_cir()}}, \code{\link[=aci_fit]{aci_fit()}}
}
