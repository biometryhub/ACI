% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-model.R
\name{aci_simulate}
\alias{aci_simulate}
\title{Simulate a conditional Gaussian nonlinear system}
\usage{
aci_simulate(
  model,
  n,
  dt = 0.001,
  seed = NULL,
  scheme = c("euler_maruyama", "milstein"),
  sigma_x = NULL,
  d_sigma_x = NULL,
  increments = NULL
)
}
\arguments{
\item{model}{An \code{aci_model} object; see \code{\link[=aci_cgns_model]{aci_cgns_model()}}.}

\item{n}{Integer scalar. The number of time steps to simulate, including the
initial step; at least two.}

\item{dt}{Numeric scalar. The integration time step; must be positive.}

\item{seed}{Optional integer. If supplied, seeds a reproducible path and
restores the caller's generator state on exit.}

\item{scheme}{Character scalar. The integration scheme,
\code{"euler_maruyama"} (the default) or \code{"milstein"}. The two coincide unless
the diffusion varies with the state it integrates, so \code{"milstein"}
requires \code{sigma_x} and \code{d_sigma_x}.}

\item{sigma_x}{Optional function of the observed state. Supplying it makes
the observation noise multiplicative, overriding the model's constant
\code{S_xoS_x} \strong{for the simulation only}. This is a simulation capability:
the filter still requires a constant observation-noise covariance, so a
path generated this way cannot yet be assimilated by this package.}

\item{d_sigma_x}{Optional function of the observed state, the derivative of
\code{sigma_x}. Required by the Milstein scheme, and never estimated
numerically on the caller's behalf.}

\item{increments}{Optional named list of the standard normal variates to
integrate, with elements \code{dW_x} and \code{dW_y}, each a complete finite numeric
vector of length \code{n - 1}. Supplying it drives the integrator with those
variates instead of drawing any, which is how a path captured from another
implementation is reproduced here. The Wiener increment applied at a step
is \code{sqrt(dt)} times the variate, following the reference implementation's
own naming. Cannot be combined with \code{seed}.}
}
\value{
A data frame with \code{n} rows and columns \code{t} (time), \code{x} (the
observed process) and \code{y} (the unobserved process).
}
\description{
Simulates a realisation of the observed and unobserved processes of an
\code{aci_model} by an Euler-Maruyama integration of its pair of stochastic
differential equations. The simulation starts from the model's initial state
and draws independent standard normal increments at each step.
}
\details{
The random increments come from R's generator, which differs from that of
the reference MATLAB implementation, so a simulated path here does not
reproduce the reference path even at a matching seed. This is expected: the
independent-oracle grade of the numerical core is run on the MATLAB signal
itself, not on a fresh simulation.

Supplying \code{seed} makes the path reproducible without disturbing the calling
session: the generator state is saved before the draw and restored when the
function exits, so a seeded call has no effect on the sequence a caller
would otherwise have seen. Leaving \code{seed} as \code{NULL} consumes the global
stream in the ordinary way.

What a matching seed cannot buy, supplied increments can. \code{increments}
takes the standard normal variates the integrator would otherwise draw, one
per transition in each of \code{dW_x} and \code{dW_y}, and no random numbers are
generated at all. This is what makes the integration scheme gradeable
against another implementation. Euler-Maruyama is invertible, so the
variates a reference run used are recoverable from its own captured path by
subtracting the drift and dividing by the noise coefficient; feeding them
back here reproduces that path itself, rather than merely a path with the
same statistics. Supplying \code{increments} together with \code{seed} is an error,
because the seed would govern nothing.

Simulation supports independent noise, that is a model with zero noise
cross-covariance.
}
\examples{
model <- aci_dyad_model()
sim <- aci_simulate(model, n = 5000, seed = 333)
head(sim)

# A seeded call leaves the caller's random stream untouched.
set.seed(1)
before <- runif(1)
set.seed(1)
invisible(aci_simulate(model, n = 10, seed = 99))
identical(before, runif(1))

# Supplied increments make the path a pure function of its input, which is
# how a run captured elsewhere is reproduced here rather than only matched
# in distribution.
set.seed(7)
z <- list(dW_x = rnorm(999), dW_y = rnorm(999))
driven <- aci_simulate(model, n = 1000, increments = z)
identical(driven$x, aci_simulate(model, n = 1000, increments = z)$x)

}
\references{
Andreou, M., Chen, N. and Bollt, E. (2026). Assimilative causal inference.
\emph{Nature Communications}, 17, 1854. \doi{10.1038/s41467-026-68568-0}
}
\seealso{
\code{\link[=aci_dyad_model]{aci_dyad_model()}}, \code{\link[=aci]{aci()}}
}
