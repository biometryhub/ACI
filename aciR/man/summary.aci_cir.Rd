% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-cir-methods.R
\name{summary.aci_cir}
\alias{summary.aci_cir}
\alias{print.summary.aci_cir}
\title{Summarise a causal influence range}
\usage{
\method{summary}{aci_cir}(object, ...)

\method{print}{summary.aci_cir}(x, ...)
}
\arguments{
\item{object}{An object of class \code{aci_cir}.}

\item{...}{Ignored.}

\item{x}{An object of class \code{summary.aci_cir}.}
}
\value{
An object of class \code{summary.aci_cir}.
}
\description{
Reports the range alongside the two diagnostics a reader needs in order to
interpret it. The first is how many reported times are censored rather than
resolved. The second is how often the divergence sequence is monotone, which
matters because \code{objective} and \code{objective_exact} are the same functional
only when it is.
}
\examples{
model <- aci_dyad_model()
sim <- aci_simulate(model, n = 900, seed = 1)
comp <- aci_dyad_components(sim$x, model$parameters)
summary(aci_cir(sim$x, comp, dt = 0.001, window = 50:150,
                mu0 = model$y0, R0 = 0.1))

}
