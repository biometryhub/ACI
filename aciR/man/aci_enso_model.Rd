% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enso-model.R
\name{aci_enso_model}
\alias{aci_enso_model}
\title{Stochastic ENSO model}
\usage{
aci_enso_model(p = aci_enso_parameters())
}
\arguments{
\item{p}{A parameter list, as returned by \code{\link[=aci_enso_parameters]{aci_enso_parameters()}}.}
}
\value{
A list describing the model, with its \code{label}, \code{parameters}, the
names of its \code{observed} and \code{unobserved} components, and the \code{components}
function that builds a components list from observed paths.
}
\description{
Returns the ENSO model as a labelled description of its structure and
parameters. Unlike the scalar models, this one carries no simulator. The
six-dimensional system is integrated with a mixed scheme in the reference
implementation, and its wind-burst update contains a correction this package
does not reproduce. See the design note on that anomaly before simulating
the system by any route.
}
\details{
Use \code{\link[=aci_enso_components]{aci_enso_components()}} with observed paths to obtain the components
consumed by \code{\link[=aci_filter]{aci_filter()}}, \code{\link[=aci_smoother]{aci_smoother()}} and \code{\link[=aci_metric]{aci_metric()}}, and
\code{\link[=aci_conditional]{aci_conditional()}} to ask the causal question of one observable given the
others.
}
\examples{
model <- aci_enso_model()
model$observed
model$unobserved

}
\references{
Andreou, M., Chen, N. and Bollt, E. (2026). Assimilative causal inference.
\emph{Nature Communications}, 17, 1854. \doi{10.1038/s41467-026-68568-0}
}
\seealso{
\code{\link[=aci_enso_components]{aci_enso_components()}}, \code{\link[=aci_conditional]{aci_conditional()}}
}
