% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-model.R
\name{aci_dyad_model}
\alias{aci_dyad_model}
\title{Nonlinear dyad model}
\usage{
aci_dyad_model(
  d_x = 0.5,
  d_y = 0.5,
  gamma = 2,
  F_x = 0.5,
  F_y = 1,
  sigma_x = 0.5,
  sigma_y = 1
)
}
\arguments{
\item{d_x}{Numeric scalar. Damping of the observed process.}

\item{d_y}{Numeric scalar. Damping of the unobserved process.}

\item{gamma}{Numeric scalar. Strength of the quadratic coupling.}

\item{F_x}{Numeric scalar. Constant forcing of the observed process.}

\item{F_y}{Numeric scalar. Constant forcing of the unobserved process.}

\item{sigma_x}{Numeric scalar. Noise coefficient of the observed process;
must be non-zero, since its square is the observation-noise covariance.}

\item{sigma_y}{Numeric scalar. Noise coefficient of the unobserved process.}
}
\value{
An \code{aci_model} object; see \code{\link[=aci_cgns_model]{aci_cgns_model()}}.
}
\description{
Builds the \code{aci_model} object for the nonlinear dyad model with intermittent
extreme events, the flagship conditional Gaussian nonlinear system of the
package. The observed process \code{x} couples to the unobserved process \code{y}
through the state-dependent term \verb{gamma x}, which makes \code{x} intermittently
extreme, and the unobserved process has constant self-drift \code{-d_y}.
}
\details{
The two governing equations are
\verb{dx = (- d_x x + gamma x y + F_x) dt + sigma_x dW_x} and
\verb{dy = (- d_y y - gamma x^2 + F_y) dt + sigma_y dW_y}, with independent noise.
The default parameters are those of the reference implementation.
}
\examples{
model <- aci_dyad_model()
model
sim <- aci_simulate(model, n = 5000, seed = 333)
fit <- aci(sim$x, model)
summary(fit)

}
\references{
Andreou, M., Chen, N. and Bollt, E. (2026). Assimilative causal inference.
\emph{Nature Communications}, 17, 1854. \doi{10.1038/s41467-026-68568-0}
}
\seealso{
\code{\link[=aci_cgns_model]{aci_cgns_model()}}, \code{\link[=aci_simulate]{aci_simulate()}}, \code{\link[=aci]{aci()}}
}
