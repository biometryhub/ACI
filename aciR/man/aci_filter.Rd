% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-core.R
\name{aci_filter}
\alias{aci_filter}
\title{Forward conditional Gaussian filter}
\usage{
aci_filter(x, comp, dt, mu0, R0)
}
\arguments{
\item{x}{Numeric vector. The observed signal, one value per time step.}

\item{comp}{A conditional Gaussian components list; see \link{aci_components}.}

\item{dt}{Numeric scalar. The integration time step.}

\item{mu0}{Numeric scalar. The initial filtered mean of the unobserved
component.}

\item{R0}{Numeric scalar. The initial filtered covariance of the unobserved
component.}
}
\value{
A list with two numeric vectors, \code{mean} and \code{cov}, the filtered mean
and covariance of the unobserved component at each time step.
}
\description{
Runs the forward filter of a conditional Gaussian nonlinear system. Given an
observed signal and the system components, it returns the filtered mean and
covariance of the unobserved component at each time step, that is the mean
and covariance of the unobserved state conditional on the observed path up
to and including the current step.
}
\details{
The recursion is the closed-form conditional Gaussian filter and is
integrated with a first-order (Euler) step of width \code{dt}.
}
\examples{
p <- list(
  d_x = 0.5, d_y = 0.5, gamma = 2, F_x = 0.5, F_y = 1,
  sigma_x = 0.5, sigma_y = 1
)
set.seed(1)
sim <- aci_simulate_dyad(n = 2000, p = p)
comp <- aci_dyad_components(sim$x, p)
filt <- aci_filter(sim$x, comp, dt = 0.001, mu0 = p$F_y / p$d_y, R0 = 0.1)
str(filt)

}
\references{
Andreou, M., Chen, N. and Bollt, E. (2026). Assimilative causal inference.
\emph{Nature Communications}, 17, 1854. \doi{10.1038/s41467-026-68568-0}
}
\seealso{
\code{\link[=aci_smoother]{aci_smoother()}}, \code{\link[=aci_metric]{aci_metric()}}
}
