% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dyad-model.R
\name{aci_simulate_dyad}
\alias{aci_simulate_dyad}
\title{Simulate the nonlinear dyad model}
\usage{
aci_simulate_dyad(
  n,
  p = list(d_x = 0.5, d_y = 0.5, gamma = 2, F_x = 0.5, F_y = 1, sigma_x = 0.5, sigma_y =
    1),
  dt = 0.001
)
}
\arguments{
\item{n}{Integer scalar. The number of time steps to simulate, including the
initial step.}

\item{p}{A named list of dyad parameters with elements \code{d_x}, \code{d_y},
\code{gamma}, \code{F_x}, \code{F_y}, \code{sigma_x} and \code{sigma_y}. The default is the
parameter set of the reference model.}

\item{dt}{Numeric scalar. The integration time step.}
}
\value{
A list with three numeric vectors, \code{t} (time), \code{x} (the observed
process) and \code{y} (the unobserved process), each of length \code{n}.
}
\description{
Simulates a realisation of the nonlinear dyad model with intermittent
extreme events by an Euler-Maruyama integration of its pair of stochastic
differential equations. The observed process \code{x} responds to the unobserved
process \code{y} through a state-dependent coupling, which produces intermittent
bursts in \code{x}.
}
\details{
The simulation draws standard normal increments and is therefore stochastic;
set a seed with \code{\link[=set.seed]{set.seed()}} before calling for a reproducible path. The
initial conditions are the deterministic equilibria \code{F_x / d_x} and
\code{F_y / d_y}.
}
\examples{
set.seed(1)
sim <- aci_simulate_dyad(n = 2000)
str(sim)

}
\references{
Andreou, M., Chen, N. and Bollt, E. (2026). Assimilative causal inference.
\emph{Nature Communications}, 17, 1854. \doi{10.1038/s41467-026-68568-0}
}
\seealso{
\code{\link[=aci_dyad_components]{aci_dyad_components()}}
}
