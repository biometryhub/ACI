% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-methods.R
\name{plot.aci}
\alias{plot.aci}
\title{Plot an assimilative causal inference result}
\usage{
\method{plot}{aci}(x, y, ...)
}
\arguments{
\item{x}{An \code{aci} object, as returned by \code{\link[=aci]{aci()}}.}

\item{y}{Ignored, for compatibility with \code{\link[=plot]{plot()}}.}

\item{...}{Further graphical parameters passed to the underlying \code{\link[=plot]{plot()}}
calls.}
}
\value{
The result \code{x}, invisibly. Called for the plot it draws.
}
\description{
Draws the observed signal and the causal-information metric on a shared time
axis, the pair of panels that carries the method's reading: bursts in the
observed signal above, and the steps at which the future of that signal
sharpens the estimate of the unobserved component below.
}
\details{
The method uses base graphics so that plotting costs the package no
dependency. The colours are chosen to remain distinguishable in the common
forms of colour vision deficiency. For a publication-grade figure, take
\code{\link[=as.data.frame]{as.data.frame()}} of the result and draw it with the grammar of graphics;
the \emph{Assimilative causal inference on the nonlinear dyad model} vignette
shows the recipe.
}
\examples{
model <- aci_dyad_model()
fit <- aci(aci_simulate(model, n = 5000, seed = 333)$x, model)
plot(fit)

}
\seealso{
\code{\link[=aci]{aci()}}, \code{\link[=as.data.frame.aci]{as.data.frame.aci()}}
}
