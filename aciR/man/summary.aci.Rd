% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-methods.R
\name{summary.aci}
\alias{summary.aci}
\title{Summarise an assimilative causal inference result}
\usage{
\method{summary}{aci}(object, ...)
}
\arguments{
\item{object}{An \code{aci} object, as returned by \code{\link[=aci]{aci()}}.}

\item{...}{Ignored, for compatibility with \code{\link[=summary]{summary()}}.}
}
\value{
An object of class \code{summary.aci}: a list with the number of steps
\code{n}, the step \code{dt}, the time \code{span}, the model \code{label}, the metric
five-number summary \code{metric}, the peak metric value \code{peak} and its time
\code{peak_time}, the smallest covariances \code{min_filter_cov} and
\code{min_smoother_cov}, the \code{terminal_residual}, and the number of
round-off-clamped metric values \code{n_clamped}.
}
\description{
Summarises an \code{aci} object into the quantities a reader needs to judge the
result rather than only to read it: the distribution of the
causal-information metric, where and when it peaks, and the diagnostics that
say whether the numerical assumptions held.
}
\details{
Three diagnostics are reported alongside the metric. The smallest filtered
and smoothed covariances indicate how close the explicit Euler
discretisation came to losing positivity; a value near zero means the result
is sensitive to \code{dt}. The terminal residual is the gap between the smoother
and the filter at the final step, which is zero analytically and so
measures accumulated numerical error. The clamp count reports how many
metric values were negative by no more than round-off and were clamped to
zero, which can happen where the two posteriors agree to near machine
precision. The exact zero at the final step is the terminal identity at
work, not a clamp, and is not counted.
}
\examples{
model <- aci_dyad_model()
fit <- aci(aci_simulate(model, n = 5000, seed = 333)$x, model)
summary(fit)

}
\references{
Andreou, M., Chen, N. and Bollt, E. (2026). Assimilative causal inference.
\emph{Nature Communications}, 17, 1854. \doi{10.1038/s41467-026-68568-0}
}
\seealso{
\code{\link[=aci]{aci()}}, \code{\link[=plot.aci]{plot.aci()}}, \code{\link[=as.data.frame.aci]{as.data.frame.aci()}}
}
