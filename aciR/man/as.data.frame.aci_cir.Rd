% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-cir-methods.R
\name{as.data.frame.aci_cir}
\alias{as.data.frame.aci_cir}
\title{Coerce a causal influence range to a data frame}
\usage{
\method{as.data.frame}{aci_cir}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x}{An object of class \code{aci_cir}.}

\item{row.names, optional}{Passed through to the data frame.}

\item{...}{Ignored.}
}
\value{
A data frame with one row per reported time.
}
\description{
The per-time quantities only. The subjective matrix is left behind because
it is threshold-by-time and does not belong in the same rectangle.
}
\details{
\code{monotone} is carried because it is the condition under which \code{objective}
and \code{objective_exact} are the same functional, and it is what
\code{\link[=summary.aci_cir]{summary.aci_cir()}} tells the reader to look at; a diagnostic reachable only
through a print method is not much of a diagnostic.
}
\examples{
model <- aci_dyad_model()
sim <- aci_simulate(model, n = 900, seed = 1)
comp <- aci_dyad_components(sim$x, model$parameters)
head(as.data.frame(aci_cir(sim$x, comp, dt = 0.001, window = 50:150,
                           mu0 = model$y0, R0 = 0.1)))

}
