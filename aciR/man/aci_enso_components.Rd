% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enso-model.R
\name{aci_enso_components}
\alias{aci_enso_components}
\title{Conditional Gaussian components of the stochastic ENSO model}
\usage{
aci_enso_components(
  T_C,
  T_E,
  I,
  p = aci_enso_parameters(),
  time = NULL,
  dt = 0.005
)
}
\arguments{
\item{T_C, T_E, I}{Numeric vectors of equal length. The observed
central-Pacific temperature anomaly, eastern-Pacific temperature anomaly,
and Walker circulation strength.}

\item{p}{A parameter list, as returned by \code{\link[=aci_enso_parameters]{aci_enso_parameters()}}.}

\item{time}{Numeric vector or \code{NULL}. The observation times, needed because
the coefficients are seasonally modulated. When \code{NULL}, a regular grid of
spacing \code{dt} starting at zero is used.}

\item{dt}{Numeric scalar. The step used to build the default \code{time} grid.}
}
\value{
A vector-valued conditional Gaussian components list; see
\link{aci_components}.
}
\description{
Builds the vector-valued components list of the ENSO model from the observed
paths, for the configuration in which the central- and eastern-Pacific
temperature anomalies and the Walker circulation are observed and the zonal
current, thermocline depth and wind burst are not.
}
\details{
The observed state is \verb{x = (T_C, T_E, I)} and the unobserved state is
\verb{y = (u, h_W, tau)}, both three-dimensional. Every coefficient varies with
the observed state, the season, or both.

Two noise covariances vary in time and neither is optional. The Walker
circulation's observation noise is multiplicative in its own state, with
variance \code{lambda * (4 - I) * I}, which vanishes at the ends of the interval
\verb{[0, 4]} and so keeps the process inside it. The latent noise of the wind
burst depends on the observed central-Pacific temperature and the season. A
filter that read either at its first step alone would be integrating a
different system.

Because the Walker-circulation noise vanishes at \code{I = 0} and \code{I = 4}, an
observed path that reaches either endpoint gives a singular observation-noise
covariance. Such a path is rejected rather than regularised: the filter
inverts that covariance, and quietly nudging it would be inventing an
observation the data does not support.
}
\examples{
p <- aci_enso_parameters()
n <- 200
tt <- seq(0, by = 0.005, length.out = n)
comp <- aci_enso_components(
  T_C = 0.05 * sin(tt), T_E = 0.04 * cos(tt),
  I = 1.6 + 0.2 * sin(0.5 * tt), p = p, time = tt
)
dim(comp$L_x)

}
\references{
Andreou, M., Chen, N. and Bollt, E. (2026). Assimilative causal inference.
\emph{Nature Communications}, 17, 1854. \doi{10.1038/s41467-026-68568-0}
}
\seealso{
\code{\link[=aci_enso_parameters]{aci_enso_parameters()}}, \code{\link[=aci_enso_model]{aci_enso_model()}}, \code{\link[=aci_conditional]{aci_conditional()}}
}
