% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predprey-model.R
\name{aci_predprey_model}
\alias{aci_predprey_model}
\title{Noisy predator-prey model}
\usage{
aci_predprey_model(
  direction = "prey_to_predator",
  alpha = 0.4,
  beta = 0.1,
  gamma = 1.1,
  delta = 0.4,
  sigma_x = 0.3,
  sigma_y = 0.3,
  x0 = 4,
  y0 = 4
)
}
\arguments{
\item{direction}{Character scalar, \code{"prey_to_predator"} (the default) or
\code{"predator_to_prey"}. See \code{\link[=aci_predprey_components]{aci_predprey_components()}}.}

\item{alpha}{Numeric scalar. The predator's natural death rate.}

\item{beta}{Numeric scalar. The effect of prey availability on predator
growth.}

\item{gamma}{Numeric scalar. The prey's natural growth rate.}

\item{delta}{Numeric scalar. The effect of predator presence on prey
decline.}

\item{sigma_x, sigma_y}{Numeric scalars. The noise amplitudes of the predator
and prey processes.}

\item{x0, y0}{Numeric scalars. Initial predator and prey populations.}
}
\value{
An object of class \code{aci_model}.
}
\description{
Builds the stochastic Lotka-Volterra predator-prey model as an \code{aci_model}
object, ready for \code{\link[=aci_simulate]{aci_simulate()}} and \code{\link[=aci]{aci()}}. The parameter defaults are
those of the reference implementation.
}
\details{
The model is studied in two directions, and \code{direction} selects which. The
two are genuinely different questions rather than one question and its
mirror. The observed process differs, the latent process differs, and the
causal-information metric is not symmetric between them.
}
\examples{
model <- aci_predprey_model()
model

}
\references{
Andreou, M., Chen, N. and Bollt, E. (2026). Assimilative causal inference.
\emph{Nature Communications}, 17, 1854. \doi{10.1038/s41467-026-68568-0}
}
\seealso{
\code{\link[=aci_dyad_model]{aci_dyad_model()}}, \code{\link[=aci_predprey_components]{aci_predprey_components()}}, \code{\link[=aci]{aci()}}
}
