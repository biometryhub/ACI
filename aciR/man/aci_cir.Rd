% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-cir.R
\name{aci_cir}
\alias{aci_cir}
\title{Causal influence range}
\usage{
aci_cir(
  x,
  comp,
  dt,
  filt = NULL,
  window = NULL,
  epsilon = 10^seq(-6, 0.5, length.out = 129L),
  threshold = 1e-05,
  margin = 0.1,
  horizon = NULL,
  mu0 = NULL,
  R0 = NULL
)
}
\arguments{
\item{x}{Numeric vector. The observed signal, one value per time step.}

\item{comp}{A conditional Gaussian components list; see \link{aci_components}.}

\item{dt}{Numeric scalar. The integration time step; must be positive.}

\item{filt}{A list with numeric vectors \code{mean} and \code{cov}, as returned by
\code{\link[=aci_filter]{aci_filter()}}. When \code{NULL}, the filter is run internally, which requires
\code{mu0} and \code{R0}.}

\item{window}{Integer vector or \code{NULL}. Indices of the time steps at which
the range is reported. When \code{NULL}, the whole signal is used, which is
quadratic in its length and is rarely what is wanted for a long record.}

\item{epsilon}{Numeric vector. Thresholds at which the subjective range is
evaluated, in nats. Defaults to a 129-point logarithmic grid from \code{1e-6}
to \code{10^0.5}. The reference implementation spans the same range with 513
points; the cheaper grid is this package's default, and
\code{10^seq(-6, 0.5, length.out = 513L)} reproduces the reference's
\code{objective_exact}.}

\item{threshold}{Numeric scalar. A peak divergence below this value is
treated as no detectable influence, and the objective range is reported as
zero rather than as the ratio of two negligible quantities. Defaults to
\code{1e-5}.}

\item{margin}{Numeric scalar in \verb{(0, 1)}. The fraction of each comparison
sequence that must remain unused for the range at that time to count as
resolved. A time whose range consumes more than \code{1 - margin} of the record
available after it is marked saturated and its ranges returned as \code{NA}.
Defaults to \code{0.1}.}

\item{horizon}{Integer scalar or \code{NULL}. How many steps of the record each
reported time may look forward across, counted from the start of the
record rather than from the reported time. \code{NULL}, the default, uses the
whole record.

The reference implementation truncates this comparison at the end of its
reporting window, which biases both the integral and the range low near
that end; supplying the same value here reproduces its numbers. The
truncation applies only to how far forward the comparison looks, never to
the fully informed posterior it is compared against, which is always taken
over the whole record.}

\item{mu0, R0}{Numeric scalars. Initial filtered mean and covariance, used
only when \code{filt} is \code{NULL}.}
}
\value{
An object of class \code{aci_cir}, a list with the reported \code{time}, the
\code{objective} range at each time, the \code{objective_exact} range obtained by
integrating the subjective ranges over the whole threshold grid rather
than by the efficient approximation, the \code{subjective} range as a matrix with
one row per threshold and one column per time, the \code{epsilon} grid, the
\code{peak} divergence at each time, the logical matrix \code{subjective_censored}
marking thresholds whose range ran past the retained margin, the character
\code{status} (\code{"resolved"}, \code{"censored"}, \code{"below_threshold"} or
\code{"insufficient"}), the logical \code{monotone} marking times whose divergence
sequence decreases with lag -- the condition under which \code{objective} and
\code{objective_exact} are the same functional -- and the logical \code{saturated},
which is \code{status == "censored"}.
}
\description{
Computes the subjective and objective causal influence range of the
unobserved component on the observed signal. The causal influence range
measures how far forward in the observed record one must look before the
estimate of the unobserved state at a given time stops improving, and so
complements the causal-information metric, which measures how much it
improves in total.
}
\details{
At each reported time the function forms the relative entropy between the
online-smoother posterior informed by the whole record and the posterior
informed only up to each later observation. That sequence decays as the
later observation advances. The \strong{subjective} range at a threshold is the
elapsed time after which the sequence stays below that threshold; the
\strong{objective} range is the threshold-free summary obtained by integrating
the sequence and normalising by its peak, which the source paper gives as a
computationally efficient underestimate of the range defined by averaging
the subjective ranges over all thresholds.

The computation is quadratic in the length of \code{window}, because every
reported time is compared against every later observation. Choose the window
accordingly: a few thousand steps is comfortable, and reproducing a
figure at the scale of the published one is a batch computation rather than
an interactive one.

A reported time close to the end of the record cannot be resolved, because
the observations that would settle it do not exist. Such a time is not
unmeasured, though: its range is \strong{right-censored}, and the truncated value
is a lower bound on the true one. The result therefore returns the value and
records the caveat in \code{status}, rather than discarding what the record does
support. Only a time with fewer than three later observations, where no
quadrature is possible at all, returns \code{NA}.

\code{objective} and \code{objective_exact} are different functionals, not two
quadratures of one. They coincide when the divergence decreases with lag, by
the layer-cake identity. Both this package and the reference measure the
range as the \strong{last} time the divergence exceeds a threshold, which is at
least the measure of the superlevel set and is strictly larger the moment
the sequence is not monotone -- which is the common case on a truncated
\code{horizon}. Seeing \code{objective} below \code{objective_exact} is that definitional
gap, not a numerical defect in either.
}
\examples{
model <- aci_dyad_model()
sim <- aci_simulate(model, n = 600, seed = 1)
comp <- aci_dyad_components(sim$x, model$parameters)
rng <- aci_cir(sim$x, comp, dt = 0.001, window = 50:250,
               mu0 = model$y0, R0 = 0.1)
summary(rng$objective)

}
\references{
Andreou, M., Chen, N. and Bollt, E. (2026). Assimilative causal inference.
\emph{Nature Communications}, 17, 1854. \doi{10.1038/s41467-026-68568-0}

Andreou, M., Chen, N. and Li, Y. (2026). An adaptive online smoother with
closed-form solutions and information-theoretic lag selection for
conditional Gaussian nonlinear systems. \emph{Journal of Nonlinear Science}.
\doi{10.48550/arXiv.2411.05870}
}
\seealso{
\code{\link[=aci_online_smoother]{aci_online_smoother()}}, \code{\link[=aci_metric]{aci_metric()}}
}
