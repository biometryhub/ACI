% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predprey-model.R
\name{aci_predprey_components}
\alias{aci_predprey_components}
\title{Conditional Gaussian components of the noisy predator-prey model}
\usage{
aci_predprey_components(x, p, direction)
}
\arguments{
\item{x}{Numeric vector. The observed signal, one value per time step. Which
population this is depends on \code{direction}.}

\item{p}{A named list with elements \code{alpha}, \code{beta}, \code{gamma}, \code{delta},
\code{sigma_x} and \code{sigma_y}, each a finite numeric scalar. The \code{parameters}
entry of an \code{\link[=aci_predprey_model]{aci_predprey_model()}} is exactly this list.}

\item{direction}{Character scalar. \code{"predator_to_prey"} observes the prey and
treats the predator as latent, asking whether the predator influences the
future of the prey. \code{"prey_to_predator"} is the converse.}
}
\value{
A conditional Gaussian components list; see \link{aci_components}.
}
\description{
Builds the conditional Gaussian components list for a stochastic
Lotka-Volterra predator-prey pair, given an observed signal, the model
parameters, and which of the two causal directions is being studied.
}
\details{
The system is
\deqn{\mathrm{d}x = (\beta x y - \alpha x)\,\mathrm{d}t +
  \sigma_x\,\mathrm{d}W_x}
\deqn{\mathrm{d}y = (\gamma y - \delta x y)\,\mathrm{d}t +
  \sigma_y\,\mathrm{d}W_y}
with \code{x} the predator and \code{y} the prey. Either variable may be taken as the
observed one, and the choice determines which causal question is asked.

In both directions the latent variable's self-drift depends on the observed
state, so \code{L_y} is a vector rather than a scalar. This is what distinguishes
the model from the dyad, whose latent self-drift is the constant \code{-d_y}.
}
\examples{
model <- aci_predprey_model("prey_to_predator")
sim <- aci_simulate(model, n = 2000, dt = 0.005, seed = 1)
comp <- aci_predprey_components(sim$x, model$parameters, model$direction)
str(comp)

}
\references{
Andreou, M., Chen, N. and Bollt, E. (2026). Assimilative causal inference.
\emph{Nature Communications}, 17, 1854. \doi{10.1038/s41467-026-68568-0}
}
\seealso{
\link{aci_components}, \code{\link[=aci_predprey_model]{aci_predprey_model()}}, \code{\link[=aci_dyad_components]{aci_dyad_components()}}
}
