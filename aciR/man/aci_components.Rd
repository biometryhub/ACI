% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aci-core.R
\name{aci_components}
\alias{aci_components}
\title{Conditional Gaussian components list}
\description{
Several functions in aciR share a \code{comp} argument, a named list holding the
per-step coefficients of a conditional Gaussian nonlinear system (CGNS). For
an observed process \code{x} and an unobserved (conditionally Gaussian) process
\code{y}, the coefficients are those of the pair of stochastic differential
equations governing \code{x} and \code{y}. The list has the following entries.
}
\details{
\describe{
\item{\code{L_x}}{Numeric vector, length \code{n}. The coupling of the unobserved
component into the drift of the observed process at each time step.}
\item{\code{f_x}}{Numeric vector, length \code{n}. The remaining drift of the
observed process at each time step.}
\item{\code{L_y}}{Numeric scalar, or numeric vector of length \code{n}. The linear
self-drift of the unobserved component. A scalar is a self-drift
constant in time, as in the supplied dyad model; a vector carries one
value per observation, as the supplied predator-prey model needs. It may
depend on the observed signal but never on the unobserved component,
which is what keeps the system conditionally Gaussian.}
\item{\code{f_y}}{Numeric vector, length \code{n}. The remaining drift of the
unobserved process at each time step.}
\item{\code{S_xoS_x}}{Numeric scalar. The observation-noise covariance,
the product of the observed-process noise coefficient with its
transpose. Must be strictly positive.}
\item{\code{S_yoS_y}}{Numeric scalar. The latent-noise covariance of the
unobserved process. Must be non-negative.}
\item{\code{S_yoS_x}}{Numeric scalar. The latent-to-observation noise
cross-covariance.}
\item{\code{S_xoS_y}}{Numeric scalar. The observation-to-latent noise
cross-covariance, the transpose of \code{S_yoS_x}. For the scalar systems
this package integrates the two are equal, and a components list in
which they disagree is rejected.}
}

The joint noise covariance must be positive semidefinite, which for a
scalar system means \code{S_xoS_x * S_yoS_y - S_yoS_x^2} is non-negative. A
components list that violates any of these conditions is rejected before
the recursion starts.

This schema is the package's extension surface for advanced use. Build a
components list directly to run the core on a conditional Gaussian system
for which aciR supplies no constructor. See \code{\link[=aci_dyad_components]{aci_dyad_components()}} for a
worked example, and \code{\link[=aci_cgns_model]{aci_cgns_model()}} for the higher-level alternative.
}
\keyword{internal}
